# 담당자 상시 지시 대장

한림제약 QA 담당자가 준 지시를 모아 둔 곳입니다. **모든 PQR 에 적용되는 규칙**이며,
한 번 말한 것은 다시 말하지 않아도 다음 제품부터 그대로 지켜야 합니다.

읽는 법 — **PQR 작업을 시작할 때 이 파일을 먼저 읽으세요.** 각 줄은 이렇게 되어 있습니다:

> **지시** (담당자 말) → **반영** (어떻게 지키는가) → `코드 위치`

새 지시를 받으면 **여기에 한 줄 더 적고** 코드에 반영하세요. 코드만 고치고 여기 안 적으면
다음 작업에서 왜 그렇게 하는지 알 수 없게 됩니다. 반대로 여기만 적고 코드에 없으면
지켜지지 않습니다 — 둘 다 해야 합니다.

---

## 첫 작성 점검표 — 새 제품 PQR 을 처음 쓸 때

담당자 2026-09-07 한림포비돈점안액(점안제 첫 제품)에서 실제로 겪은 것을 그대로 옮긴 것.
**올리기 전에 이 여덟 가지만 보면 첫 판부터 거의 완성본이 나온다.**

| 지시 | 반영 |
|---|---|
| **① (u) 16. 전년도 결재본을 반드시 올린다 — 되도록 `.docx`** | 이것 하나가 9.1 시험항목·허용기준, 9.2 열 머리글, 10.2 설비 목록, 4항 일정계획, 13.3 경향표의 **뼈대**를 모두 준다. 없으면 (v) 0. 공양식이 바탕이 되는데 공양식은 빈 뼈대뿐이라 그 항들이 통째로 사선으로 남는다. `.doc` 도 되지만 Word 변환을 거치니 `.docx` 가 안전하다 |
| **② (v) 0. 당해 년도 공양식은 '이 제품 것' 인지 본다** | 공양식이 있으면 그것이 바탕이 되고 전년도는 참고만 한다. 다른 제품 코드가 적힌 양식이면 프로그램이 멈춘다 |
| **③ 6항 공 기록서는 그 제품 것인지 본다** | 이름이 남의 제품이면 프로그램이 쓰지 않고 ★ 로 알린다. 잘못 들어가면 제조단위·포장단위·8.1.3 자재가 통째로 남의 것이 된다(올로원스 기록서가 들어가 5mL 자재가 실렸다) |
| **④ 9.2.x 는 시험성적서 PDF** | ERP·LIMS 화면을 저장한 파일이 아니라 성적서 PDF 여야 한다. 압축(zip)은 그대로 올려도 된다 — 풀어서 읽는다. 점안제는 9.2.1(점안제)·9.2.2(바이오버든)로 갈라 올린다 |
| **⑤ 13 폴더에 올해 시험일지 스캔 PDF** | 없으면 전년도 13항을 그대로 옮기고 노랑으로 남는다. 올렸으면 **'안정성 판독 🔍' 을 먼저** 눌러 둔다 — 그래야 '보고서 작성' 이 1분 안팎으로 끝난다 |
| **⑥ 7. 수율현황표에 그해 모든 Lot·모든 공정** | 포장이 내수·수출로 갈린 제품이면 두 열 모두 있어야 한다. 없는 칸은 '확인 필요' 로 남는다 |
| **⑦ 이력이 없는 항은 '해당없음 확인' 으로 마감** | 11·12·14·15 는 마감해야 수집률 100% 가 되어 '보고서 작성' 이 열린다. 이 네 항은 전년도 값을 이어받지 않는다 |
| **⑧ 만든 뒤 — 문의 목록의 ★ 먼저, 그다음 노랑 칸** | ★ 는 '먼저 해 주세요'(자료를 더 올려야 하는 것). 노랑은 두 가지다: **전년도 값 그대로**이거나 **프로그램이 확신하지 못한 값**. 원본과 대조해 고치면 재작성에도 그 값이 그대로 쓰인다 |

## 0. 늘 지키는 것

| 지시 | 반영 |
|---|---|
| 실제 GMP 품질기록은 저장소에 커밋하지 않는다 | 산출물은 스크래치패드에 두고 파일로 전달. `docs/pqr/data.js`·`data.json` 은 .gitignore |
| 값을 지어내지 않는다 | 자료에 없으면 비워 두고 문의 목록에 남긴다 |
| **전년도 결재본을 읽지 못하면 보고서를 내지 않는다** | 변환을 세 번 되풀이하고 그래도 안 되면 멈춘다. 조용히 넘어가면 규격·제조단위·포장단위·실시 사유가 빈 채로 사선만 그어진 보고서가 나간다(2026-09 디겐타에서 실제로 생김) |
| 서명란(성명·서명일자)은 비운다 | 결재란은 채우지 않는다 |
| 파일 작성은 요청할 때만 | "작성해줘" 라고 할 때만 문서를 만든다 |

## 1. 자료를 읽는 규칙

| 지시 | 반영 | 코드 |
|---|---|---|
| 전년도 결재본은 장비·원료 등 **참고용**이고, 올해 자료로 새로 쓴다 | 해마다 바뀌지 않는 값만 빈 칸에 이어받는다 | `engine/carry.py` |
| 제조번호 셋째 글자가 제조 연도, PQR 은 그 다음 해 (OGY301 → 2025 제조 → 2026년 PQR) | 화면이 준 기간보다 제조번호를 앞세운다 | `engine/lotcode.py` |
| 모든 PQR 은 EDMS 공양식(E-HLF-32)으로 쓴다 — 평가항목 (v) 'PQR 작성 공양식' | 공양식을 바탕으로 쓰고, 없을 때만 전년도 결재본 | `engine/edms.py` |
| 압축(ZIP)으로 올린 자료도 찾아서 읽는다 | 겉 폴더를 벗기고 항 번호로 가른다. 한글 이름은 UTF-8 → CP949 순서로 되돌린다 | `engine/collect.py` `_member_name` |

## 2. 항별 작성 규칙

| 항 | 지시 | 반영 | 코드 |
|---|---|---|---|
| 5 | 책임과 권한의 '품질보증 1팀' 은 **AQA팀** | 문구를 바꿔 쓴다 | `recipe_ointment.py` |
| 7 | 수율은 최솟값·최댓값·평균값을 적는다 | 요약행 열은 그리드 번호로 짚는다(가로 병합 때문) | `docedit.grid_cells` |
| 8.1.3 | 설명서·케이스는 **자사규격** (사선 아님) | P 코드는 자사규격으로 적는다 | `recipe_ointment._supplier` |
| 9 | 최솟값·최댓값·평균값을 적고 사선은 지운다 | 판독값에서 요약을 낸다 | `recipe_ointment.summarize` |
| 3 | **대상 제품 정보는 전년도 PQR 에서 가져온다** | 허가증 PDF 에 글자 정보가 없는 일이 잦다. 점검항목(제형·제품분류·제품명·허가번호·허가일자·사용기한·보관조건 …)을 줄 이름으로 짝지어 옮긴다 | `carry._label_columns` |
| 10.1 | PV 는 **10.1 PV Validation Master File 최신본**에서 가져오고, **전년도 PQR 도 참고**한다 | 마스터에서 평가 연도 PV 를 찾아 가장 최근 1건. 못 찾으면 전년도 결재본의 10.1 을 옮기고 문의로 남긴다 | `recipe_ointment.fill_pv`, `_carry_rows` |
| 10.2 | 제조설비 IQ·OQ 는 **생산장비 적격성 평가 마스터파일**에서 가져온다 | 최신본과 `_OLD` 파일을 합쳐 읽는다(최신본에는 PQ 만 있다) | `collect.py`, `readers/masters.py` |
| 10.3~10.5 | IQ·OQ 는 **제조지원 설비 마스터파일(IQ, OQ 정보확인)**, PQ 는 **10.5 Qualification Master File(PQ, 측정위치 타당성)** — 모두 최신 파일 | 종류마다 제 파일을 먼저 본다. QM… 문서번호도 PQ 로 본다 | `collect.py` |
| 10.3~10.5 | 액제라인만 23년도에 공사한 것은 표시해 두었으니 참고한다 | 마스터의 '사유' 를 읽어 다른 라인·다른 방 공사는 뺀다. 뺀 것은 문의 목록에 | `readers/masters.applies` |
| 10.2~10.5 | 마스터파일에 값이 **없으면 사선 처리** | 빈 칸에 사선(↗)을 긋는다 | `docedit.diag_all_empty` |
| 11 | 일탈번호가 같으면 **1건**. 일탈사항은 요점만 | 번호로 묶고 요약한다 | `recipe_ointment` 11항 |
| 12 | 변경관리는 **변경번호와 변경내용만**. 위탁사 내용 제외 | 조치사항에서 위탁사·위수탁 줄을 뺀다 | `recipe_ointment` 12항 |
| 12 | **변경통보는 삭제** — 자사 제품 내용만 쓴다 | 변경 통보 줄을 뺀다 | 같은 곳 |
| 12 | 변경사항은 **간략히** (큰 항목만) | 큰 항목만 남긴다 | `recipe_ointment.brief_change` |
| 12 | 담당자가 12항 폴더에 넣은 변경관리는 **모두 이 제품에 해당한다** | '관련 제품' 이 '안연고 전 제품'·'무균라인에서 생산하는 전 제품' 처럼 무리로 적혀 있거나, 주성분을 건드리는 변경이면 해당으로 본다. 남의 제품 이름만 적힌 것만 문의로 남긴다 | `recipe_ointment.change_covers` |
| 13.1 | 안정성 **실시 사유는 작년 것을 그대로** 가져온다 | 10.1 PV 사유에서 채운다(올해 → 없으면 전년도) | `carry.pv_reasons` |
| 13 | 안정성이 공란이면 **전년도 결재본을 참고해 작성**한 뒤, 13항 최신 안정성 파일로 갱신한다 | 올해 시험일지를 못 읽으면 전년도 13.1·13.3 을 옮기고 '갱신하세요' 를 남긴다 | `recipe_ointment._carry_stability` |
| 13.3 | **관리 규격은 굵게 하지 않는다** (2026-09 — 앞선 '굵게' 지시를 이것이 대신함) | 그 줄을 보통 글씨로 | `docedit.unbold_row` |
| 16 | 10 Lot 미만이라 Cpk 를 안 냈다는 말은 결론에 적지 않는다 | 당연한 말은 뺀다 | `engine/conclusion.py` |
| 안연고 | 안연고 라인 중 **디겐타안연고만 건열멸균기**를 쓴다 | 다른 연고에서는 건열멸균기를 뺀다 | `recipe_ointment.DRY_HEAT` |

## 3. 차림새 (모든 표·모든 항)

| 지시 | 반영 | 코드 |
|---|---|---|
| 표 크기는 **창에 자동으로 맞춤** — 모든 표 | 본문 폭에 맞춘다 | `layout.py` |
| 같은 원료·코드는 **셀 병합**. 원료코드 글씨가 잘리지 않게 | 세로 병합하고 열 폭을 넓힌다 | `docedit` |
| 내용이 없으면 **1행만 남기고 사선** | 빈 블록을 한 줄로 줄이고 가로지르는 선 | `docedit.collapse_empty_block` |
| **'확인 필요' 는 노랑 형광펜** | 그 글자에 형광을 칠한다 | `docedit.highlight` |
| 비고에 사선이 그어졌으면 **N/A 삭제** | 사선 칸의 N/A 를 지운다 | `docedit.drop_na_in_diag_cells` |
| **표·각주 다음에 오는 작은 제목(8.1.2·8.2.2·10.2 …) 앞에는 빈 줄 한 줄** | 이미 있으면 그대로, 없으면 넣는다. 제목이 제목 바로 뒤에 오는 자리(8.2 → 8.2.1)는 붙인다. (앞서 "각주 뒤 엔터 삭제" 지시를 2026-09 에 뒤집음) | `docedit.blank_before_subheadings` |
| 표 안 간격 조정 — '같은 스타일의 단락 사이에 공백 삽입 안 함' | `contextualSpacing` 을 켠다 | `docedit.zero_cell_spacing` |
| **1·3항 제목 줄맞춤** (번호 뒤 빈칸 통일) | 글자 자리로 찾아 그 런만 고친다 | `docedit.align_section_titles` |
| **16항 아래와 17항 시작부분은 한 칸 띄운다 — 모든 항이 마찬가지** | 항 제목 앞에 빈 줄 한 줄 | `docedit.space_before_sections` |
| 목차는 **점선 길이**를 페이지 번호 왼쪽에 맞춘다 | 목차 글자 크기를 줄이지 않는다 | `docedit.table_font_size` (목차는 skip) |
| 페이지 번호(목차 쪽수)가 맞아야 한다 | PDF 로 쪽을 세어 목차 필드에 숫자를 적고, Word 가 열자마자 다시 계산해 전부 1 로 만들지 않도록 updateFields 를 없앤다 | `toc.fill_page_numbers`, `polish.clear_update_fields` |
| **목차 쪽 번호가 비거나 전부 1 이면 안 된다** | PDF 로 쪽을 세어 필드 결과에 숫자를 직접 적고 dirty 표시를 뗀다. settings 의 `updateFields` 는 **넣지 않는다** — 켜 두면 Word 가 열자마자 쪽 나눔 전에 계산해 전부 1 이 된다 | `toc.fill_page_numbers`, `polish.clear_update_fields` |
| 7항 **최댓값·최솟값·평균은 공정 열에 맞춘다** | 그리드 열 번호로 짚는다 | `docedit.grid_cells` |
| 7항 **비고의 사선은 한 칸으로 합친다** | 자료 줄·요약 줄의 비고를 세로 병합하고 사선 하나 | `docedit.merge_remark_column` |
| 8.1.1·8.1.2 **제조원·제조업체의 빈 줄(엔터)을 지우고 가운데 맞춤** | 줄 끝 빈 문단을 없애고 가운데로 | `docedit.tidy_cell_lines` |
| **빈 표(8.2.3·11.2·14·15항)의 가로지르는 선은 행의 왼쪽 아래 모서리에서 오른쪽 위 모서리까지** — 표 밖으로 나가지 않게 | 행 높이를 '정확히' 로 못 박고 모서리끼리 잇는다. 칸 사이 세로선은 그대로 둔다 | `docedit.draw_block_line` |
| **4·16·17·18항 줄간격은 제목 포함 1.5** | 항 제목과 본문 문단 모두 | `docedit.set_section_line_spacing` |
| **4항·16항 본문은 왼쪽 2글자 들여쓰기** | 서식의 내어쓰기·첫줄 들여쓰기를 지우고 `leftChars=200` | `docedit.set_section_indent` |
| **'특이사항 (Comment)' 글은 제목 바로 아래 줄에** | 제목과 글 사이 빈 문단을 없애고, 표 안 문단마다 `beforeAutospacing`·`afterAutospacing`·`beforeLines`·`afterLines` 를 **0 으로 분명히 적는다** — 서식의 'Normal (Web)' 스타일이 물려주는 autospacing 때문에 Word 에서만 한 줄이 벌어졌다(LibreOffice 미리보기에는 안 보인다) | `docedit.tidy_comment_cells`, `docedit.zero_cell_spacing` |
| 10.2~10.5 **IQ·OQ·PQ 칸 너비는 같게** | 세 열을 같은 폭으로 나눈다 | `layout.py` |
| 10.4·10.5 **설비명은 표 안에서 나뉘지 않고 가운데** ('현재 단락을 나누지 않음') | 그 칸 문단에 `keepLines` 를 켜고 세로 가운데 | `docedit.keep_lines_in_cells` |
| **전체 줄에 사선이 그어졌으면 칸마다의 사선은 지운다** (11.2 등) | 가로지르는 선이 있는 표에서는 낱칸 사선을 빼낸다 | `docedit.diag_all_empty` |
| 12항 **변경사항의 불필요한 엔터를 지운다** | 칸 끝의 빈 문단을 없앤다 | `docedit.tidy_cell_lines` |
| 12항 **변경 내용은 굵은 글씨로 쓰지 않는다** | 첫 줄(문서번호·변경명)만 굵게, 아래 내용은 보통 | `recipe_ointment` 12항 |
| 13.1 **실시 사유가 같으면 셀병합** | 이어지는 같은 글의 칸을 세로로 합친다 | `docedit.merge_same_text` |

## 4. 결과물

| 지시 | 반영 | 코드 |
|---|---|---|
| **10 Lot 미만**: PQR 최신 워드 + 안정성 경향표 엑셀 **2개** | Cpk 파일은 만들지 않는다(QC-126) | `engine/excel_attach.py` |
| **10 Lot 이상**: Cpk 까지 같이 | Cpk 계산 파일을 더 만든다 | 같은 곳 |
| 16. 안정성시험 경향표는 **13항 자료를 참고해 최신 파일로 신규 작성** | 지난 경향표 값을 이어받고 올해 시점을 덧붙인다 | `excel_attach._write_trend` |
| HLF-QC-126-06 은 **표선 추가 + 제조번호 가운데 맞춤** | 서식 파일에서 셀 서식 번호를 읽어 쓴다 | `stability_xlsx.style_at` |
| **작성본은 제품 폴더 아래 별도 폴더('PQR 작성본')에 저장** | 워드·엑셀을 그 폴더에 만들고, 그 폴더는 다시 자료로 읽지 않는다 | `engine/collect.OUTPUT_DIR`, `server._made_dir`, `build.find_final_report` |

## 5. 대시보드

| 지시 | 반영 | 코드 |
|---|---|---|
| 폴더에서 파일을 직접 지워도 화면에 반영된다 | 폴더 지문을 견줘 바뀌면 다시 읽는다 | `server.Workspace.refresh_if_changed` |
| 평가항목 (v) '0. 당해 년도 PQR word & excel'(옛 이름 'PQR 작성 공양식') 에 공양식을 넣고 그 양식으로 보고서를 만든다 | 항목 0 번을 두고 서식으로 쓴다 | `pqr/data/config.json`, `engine/edms.py` |
| '파일 한번에 올리기' 도 **끌어오기**로 되게 한다 | 끌어다 놓는 창 + 진행률 | `docs/pqr/index.html` |
| 알집을 올리면 **압축을 풀어 항 번호를 알아내고 항목마다 녹색불** | 겉 폴더를 벗기고 항으로 갈라 표시한다 | `server.extract_bundle`, `save_bulk_file` |
| 제품 코드가 틀리면 정정한다 (레보클점안액) | 마스터의 본보기 코드를 다수 앞머리로 접는다 | `build.fold_placeholder_codes` |
| **자료가 없으면 '보고서 완료' 가 아니라 '보고서 작성'** 으로 되돌아간다 | 제품 폴더에 근거 자료가 하나도 없으면 완료로 세우지 않는다(보고서 자신·문의 목록·작성 기록은 자료가 아니다) | `build.has_source_files` |
| **'보고서 완료' 를 누르면 재작성·취소를 고른다** (2026-09) | 완성본을 바로 열지 않고 작은 창에서 보고서 재작성·완성본 열기·취소를 고르게 한다. 재작성은 같은 이름으로 덮어쓴다 | `docs/pqr/index.html` `openRedo` |
| **'완성본 열기' 는 워드·첨부 엑셀·작성본 폴더를 함께 연다** (2026-09) | 워드만 열리면 엑셀을 다시 찾아야 했다. 첨부는 'PQR 작성본' 폴더와 제품 폴더 두 곳에서 찾는다 | `server._handle_final`, `Workspace.final_attachments` |
| 머리글 **문서번호·Rev. No. 는 비워 둔다** (2026-09) | EDMS 북마크 자리다 — 채우지 않는다 | `recipe_ointment` 머리글 |
| 12항 **적용 Lot No. 가 변경요청서에 없으면 '확인 필요'** 로 둔다 (2026-09 확인) | 지어내지 않는다 | `recipe_ointment` 12항 |
| **10 Lot 이상은 Cpk 까지** — 주성분이 둘이면 **성분마다 그 성분의 규격**으로 (2026-09) | 디겐타: 플루오로메톨론 90~110, 겐타마이신 90~120. 성적서(9.2.4)의 성분별 규격을 쓴다. 16.1 표엔 '함량(성분)' | `recipe_ointment.cpk_of`, `limits["assay_parts"]`, `conclusion.low_cpk_items` |
| Cpk 계산 파일도 **성분마다 하나**, 시트 머리(제품명 C4·공정 C5·시험항목 K5)와 규격(N6/P6·P6)은 **올해 값** (2026-09) | 전년도 파일을 서식으로 물려받되 제품명·규격이 옛것으로 남지 않게 | `excel_attach.cpk_jobs` |
| 양식이 Cpk 칸과 'Cpk 판정 결과' 칸을 세로 병합해 두었으면 **풀고 쓴다** (2026-09) | 디겐타 2026 양식 함량 열 — 안 풀면 '충분' 이 숨는다 | `detail92.fill` |
| 10 Lot 이상이면 **17항 참고 자료에 경향분석 Sheet(HLF-QC-126-08·-09) 두 줄**, 미만이면 뺀다 (2026-09) | 한림 결재본 PQR25 퀴노비드안연고 — '별표 17' 줄 뒤 | `recipe_ointment._cpk_references` |
| **조치가 끝나지 않은 일탈**이 있으면 16항을 16.1 종합 + **16.2 '차년도 제품품질평가에서 확인'** 으로 (2026-09) | 완료일이 없는 일탈이 있을 때만. 한림 결재본 문안 그대로 | `conclusion.deviation_sentence` |
| 9.2 표가 한 쪽을 넘으면 **요약 5줄(최댓값~Cpk 판정)이 다음 쪽에 홀로 남지 않게** 마지막 자료 줄과 함께 (2026-09) | 12 Lot 합성 점검 | `layout` keep_tail_together |
| 양식의 run·문단에 **rPr·pPr 이 없어도** 서식 넣기가 넘어지지 않는다 (2026-09-05 담당자 PC 작성 기록: KeyError 'r' → 요약본) | 속성 요소는 맨 앞에 만들고, 모르는 부모는 뒤에 붙인다 | `ooxml_order.place` `_HEAD` |
| 7항 **비고는 2025 결재본과 같게**: 머리글 비고는 그대로, 자료 줄들은 한 묶음에 사선 하나, 최댓값·최솟값·평균 줄은 또 한 묶음에 사선 하나 (2026-09) | 머리글에서 시작하는 긴 사선 하나로 만들지 않는다 | `docedit.merge_remark_column` |
| 11.2·14.x·15 **빈 줄 사선**은 연번~조치사항(마지막 앞 칸)까지 한 줄로 가로지르고, **CAPA 칸은 □ Yes □ No 그대로** (담당자 확인 2026-09) | 담당자가 보여 준 모양 그대로 | `docedit.draw_block_line` |
| 13.2 는 **'13.2 시판 후 안정성 시험 이력 없음.'** 한 줄로 항 번호를 붙여 쓴다 (2026-09) | 양식의 '* 시판 후 …' 줄에 번호를 붙인다 | `recipe_ointment` 13.2 |
| 프로그램이 남긴 **'…옮긴 것입니다 — 갱신하세요' 안내도 노랑** (2026-09) | 확인 필요와 같은 형광 | `layout` highlight |
| 13.1·13.3·안정성 경향 엑셀은 **올해 시험일지(13. 안정성 시험 압축)** 로 갱신한다. 손글씨라 PC 에 API 키가 없으면 **`13. 안정성시험일지 판독.json`** 을 제품 폴더에 두어야 한다 — 없으면 전년도 값이 옮겨지고 노랑 안내가 남는다 | 판독 파일이 먼저, 없으면 전년도 | `collect.stability_logs`, `vision_claude` |
| **손글씨 시험일지는 PC 프로그램이 오프라인(RapidOCR)으로 최대한 읽는다** — API 키·json·파일 전송 없이 (2026-09) | 깨끗이 읽힌 값만 쓰고, 읽지 못한 칸은 값 없이 '애매' 로 남겨 워드 노랑(13.1 완료 일자 '확인 필요'·13.3 범위 칸)·엑셀 주황(빈 칸)으로 표시하고 문의 목록에 적는다. 지난 경향표에 있는 시점은 그 값을 쓴다. 완료 일자를 못 읽으면 제조일자+기간으로 어림해 13.1 에 '확인 필요' 로 세운다. 판독 결과는 `13. 안정성시험일지 판독.json` 으로 저장되어 담당자가 고치면 그 값이 우선 | `engine/handwriting.py`, `collect` 13항, `requirements.txt` |
| 이 대장은 **팀원 누가 만들어도 같은 PQR 이 나오도록** 하는 규칙집이다 (2026-09) | 저장소(GitHub)로 공유하며, 새 지시는 여기에 먼저 적고 코드에 반영한다 | 이 파일 |
| 손글씨 시험일지는 **API·json 없이 작성자(Claude)가 직접 읽어** 값을 넣고, **글씨를 읽을 수 없을 때만 노랑**(워드)·**주황**(엑셀, 결과 칸이 원래 노랑)으로 칠한다. **지우고 고쳐 쓴 정정은 고친 값을 쓰고 표시하지 않는다**(담당자 확인 2026-09: OGX901 12M 98.7 맞음, 정정은 문제없음) | 판독 결과의 `unsure` 표시 → 13.1 완료 일자·13.3 범위 칸·경향 엑셀 칸에 표시, 문의 목록에도 적는다 | `recipe_ointment._fill_stability26`, `stability_xlsx.yellow_styles`, `excel_attach._write_trend` |

## 6. 담당자가 화면을 보며 그때그때 준 지시 (2026-09 ~)

| 지시 | 반영 | 코드 |
|---|---|---|
| 10.3~10.5 IQ·OQ: 다른 라인·다른 방 공사 문서는 빼되, **그 설비에 그 문서뿐이면 빈칸+사선 대신 그대로 넣고 문의 목록에 '이 라인에 맞는지 확인'** (2026-09-06 담당자 PC: 주사용수 분배시스템·질소 분배라인이 통째로 비었음) | 값이 있는데 비워 두지 않는다 | `recipe_ointment` 우리것() |
| 평가항목 (v) 이름은 **'0. 당해 년도 PQR word & excel'** (2026-09) | 공양식(E-HLF-32)을 두는 자리. 화면·설정 파일의 이름을 이렇게 쓴다 | `pqr/data/config.json`, `index.html` ITEMS |
| 전년도 결재본(.doc)을 PC 에서 못 바꿔도 **결재본 양식으로 만든다** — 요약본으로 떨어지지 않는다 (2026-09) | 이어받기만 건너뛰고, 문의 목록 맨 앞에 '★ …docx 로 저장해 두세요' 를 남긴다. 실패 원인 파일은 작성본 폴더에도 둔다 | `writer.write_report`, `server._handle_report` |
| 9.2 소제목 번호가 양식에서 어긋나 있으면(9.2.1·9.2.2·**9.2.1.3**) 차례대로 바로잡는다 (2026-09 점검) | 바로 위 소제목에 속하지 않는 깊은 번호만 9.2.3 으로. 9.2.1.1 같은 정상 하위 번호는 둔다 | `docedit.renumber_subheadings` |
| 9.2 질량 각주는 **그 표(포장 질량·기밀도) 바로 아래**, 번호는 양식의 '1) 모든 Lot …' **다음 번호** (2026-09 점검) | 11쪽에 '1)' 이 둘이고 다른 표 밑에 있던 것. '개개' 머리칸에 같은 번호를 윗첨자로 단다 | `recipe_ointment._note_numbers_under`, `_mark_header` |
| **판독기 설치는 `requirements-ocr.txt`(rapidocr + onnxruntime), 본체와 따로** (2026-09) | 담당자 PC 는 파이썬 3.13 — 옛 `rapidocr-onnxruntime` 은 3.12 까지라 설치가 안 됐다(install-log 확인). 새 `rapidocr`(2.x 이상) 는 `onnxruntime` 을 따로 요구하며 결과를 numpy 배열(boxes/txts/scores)로 돌려준다. 설치 실패해도 보고서는 만들고 바닥글에 '꺼짐 — 까닭' 과 Python 버전을 보인다. 오류는 `install-ocr-log.txt` | `requirements-ocr.txt`, 두 bat, `handwriting._engine_class/ocr_image/available/why` |
| **두 칸에 걸쳐 읽힌 글상자는 어느 칸의 값도 아니다** (2026-09 평가) | OGX901 6M·9M 완료일이 '2025.7.02 2025.09.10' 한 상자로 읽혀 6M 에 9M 날짜가 들어갔다. 칸 폭의 75% 이상 든 상자만 그 칸의 것으로 보고, 걸친 상자는 칸만 오려 다시 읽되 '애매' 로 둔다. 걸친 상자가 있으면 빈 칸(시험 안 함)으로 보지 않는다 | `handwriting._in_cell(strict)`, `_spilled` |
| **깨끗이 읽힌 완료 일자도 예정 시기(제조일+기간)와 동떨어지면 '확인 필요'** (2026-09 평가) | OGW701 12M '2024.08.02' 가 '2024.04.02' 로 읽혔다. 예정보다 한 달 넘게 이르거나 넉 달 넘게 늦으면, 또는 앞 시점보다 이르면 애매로 돌린다. 4장 평가 기준: 맞는 값 27, 표시 없는 오독 0 | `handwriting.read_log` due/gap |
| **업데이트·실행 bat 는 ASCII 만, 자기 자신을 TEMP 복사본으로 실행** (2026-09) | 한글 파일명·한글 문구는 cmd 에서 깨진다. 실행 중인 bat 를 덮어쓰면 'pdater'·'y' 같은 엉뚱한 명령이 돌거나 VS Code 가 열린다. 같은 내용 파일은 건너뛴다. pip 오류는 `install-log.txt`/`install-ocr-log.txt` | `PQR-업데이트.bat`, `PQR-대시보드-실행.bat`, `cli._copy_program` |
| 13.1 완료 일자 칸은 **시점마다 한 줄** — 못 읽은 시점은 줄마다 '확인 필요' (2026-09) | 한 Lot 의 시점을 모두 못 읽으면 '확인 필요' 한 줄만 들어가고 둘째 줄이 비던 것. 시험 기간 줄과 나란히 맞춘다 | `recipe_ointment._fill_stability26` |
| **작성 중에는 단추에 경과 시간, 안내에 지금 단계**("시험일지 판독 중 2/4: …") 를 보인다 (2026-09) | 첫 작성은 손글씨 시험일지를 PC 가 한 장씩 읽느라(한 장 10초~1분) 몇 분이 걸려 담당자가 멈춘 줄 알았다. 서버가 `/api/progress` 로 작성 기록 줄을 2초마다 내주고, 판독 결과는 `13. 안정성시험일지 판독.json` 에 남아 다음 작성부터는 빠르다 | `server._handle_progress`, `index.html watchProgress`, `handwriting.read_folder` |
| **10.2~10.5 IQ·OQ 를 하나로 합친 문서(IOQ20-UT-HEA5029-R)는 두 칸을 합쳐 한 번만 적는다** (담당자 2026-09-06: "10.4항과 10.5항은 너가 작성할 수 있어야 돼") | 담당자 PC 공양식은 10.4·10.5 의 합친 칸이 비어 있었고 프로그램이 합친 칸을 건너뛰어 사선만 남았다. IQ·OQ 최신 문서가 같은 한 문서면 빈 칸을 합쳐 적고, 합친 칸이 비었는데 IQ·OQ 문서가 따로면 칸을 나눠 각각 적는다. 문서번호·완료일 한쪽만 비어도 마스터로 채운다 | `recipe_ointment.update_qualification`, `_latest_pair`, `docedit.merge_right/split_span` |
| **10항 채운 뒤 검토**: 마스터파일에 문서가 있는데 빈 IQ·OQ·PQ 칸이 남으면 문의 목록 맨 앞에 ★ 로 알린다 (담당자 2026-09-06: "여러 번 말한 부분이니까 다음부터는 실수하지 않게 작성할 때 꼭 검토해 줘") | 작성 기록에 '10항 검토: … 모두 채워짐' 또는 '★ 10항 검토: 빈 칸 n' 이 남는다. 마스터에도 없는 칸(DBB5035 IQ)은 비워 두는 것이 맞으므로 잡지 않는다 | `recipe_ointment.blank_qualification_cells` |
| **손글씨 판독이 애매해도 예상값을 적고 표시한다** (담당자 2026-09-06: "판독 후 예상하는 값을 우선 적어 주고 주황색으로 표시") | 읽은 값이 있으면 그대로 적고 '애매' 표시 — 엑셀은 주황 칸에 값, 워드 13.3 은 그 해 범위 칸 노랑, 예상값이 최소·최대가 되면 그 칸도 노랑. 값을 전혀 못 읽은 칸만 빈 주황. 4장 평가: 맞음 37(그중 표시 10), 틀림-표시 3, 틀림-표시없음 0. 지난 경향표 값이 있으면 그것이 우선 | `handwriting.read_log`, `excel_attach.points_of`, `recipe_ointment._fill_stability26` |
| **애매한 손글씨에서도 예상값을 만든다** — 글자 섞인 판독('9a.4-1.'→99.4, 'qn.ay'→97.9, '1013'→101.3)을 규격 근처 값으로 추정하고 반드시 '애매' 표시 (담당자 2026-09-06: "주황색 부분에 너의 예상값을 기재하지 않았어") | 숫자가 아예 없거나 규격에서 한참 벗어나면 만들지 않는다(빈 주황 칸). 4장 평가: 맞음 41(표시 14), 틀림-표시 6, 틀림-표시없음 0, 없음 3 | `handwriting.guess_assay`, `_GUESS` |
| **옛 판독기가 만든 판독 파일은 애매한 칸만 새 판독기로 다시 읽는다** (2026-09-06) | 판독 파일에 `reader_version` 을 적고, 낮으면 unsure·빈 값만 보완한다. 깨끗이 읽혔거나 담당자가 고친 값은 그대로. 담당자 PC 가 업데이트 전 판독 파일을 그대로 써서 주황 칸이 비어 나갔다 | `handwriting.READER_VERSION/refresh_cache`, `collect` 13항 |
| **주황 칸을 비워 두지 않는다** — 세 방식(원본·오려 읽기·흑백/대비/여백 처리)으로 읽어도 규격 밖이면 헷갈리는 숫자 하나(4↔9, 1↔7, 7↔9, 3↔8 …)를 바꿔 규격 안이면 예상값으로 쓴다 (담당자 2026-09-06: "그래도 주황 칸이 비어 있는 곳이 있네") | 숫자가 아예 없는 칸만 빈 주황. 예상값이 든 칸은 지난 경향표 값과 달리 반드시 주황으로 남긴다(경향표에 있던 시점만 애매에서 뺀다). 4장 평가: 맞음 42(표시 15), 틀림-표시 8, 틀림-표시없음 0, 없음 0 | `handwriting.guess_assay/_CONFUSED/_crop_ocr_variants`, `excel_attach` known_before |
| **손글씨 판독기는 제품을 가리지 않는다** (담당자 2026-09-06: "다른 제품으로 PQR 을 작성해도 안정성 수기 글씨를 최대한 정확히 파악해서 기재") | 제조번호는 영문 2자+영숫자 4자(숫자 포함), 규격은 '90.0 ~ 110.0%'·'95 ~ 105 %' 처럼 % 가 붙은 범위만(보관 온도 '15~25℃' 는 규격이 아니다 — 잘못 잡으면 값이 통째로 사라진다), 포장은 'ng/Tube·nmL/Bottle', 보관 조건은 '온도±n°C, 습도±n%RH' 를 읽는다. 성분 이름은 그 제품 COA(924) 규격과 맞춰 붙이고, 못 맞추면 '성분1·2'. 결과는 13.1·13.3·경향 엑셀에 같은 규칙으로 들어간다 | `handwriting.LOT/SPEC/PACK/TEMP/HUMID`, `read_log` |
| **시험일지 시점은 머리칸의 손글씨 숫자로 짜임새를 가른다** — 숫자가 칸 차례(3·6·9·12·18·24…)와 맞으면 장기, 12·24·36 이면 시판 후(해마다 한 번). 못 읽은 칸은 그 짜임을 따르고, 시험 일자로 시점을 정하지 않는다(시험이 늦어져 9M 이 12.7개월에 끝난다) (2026-09-06 퀴노비드) | 한 PDF 에 Lot 이 한 쪽씩 여러 장이면 쪽마다 읽는다. 완료 일자는 맨 아래 결재 줄의 날짜(결재본 관행), 없으면 시험일자 줄. 시험구분·제품명 같은 한글은 판독기가 못 읽으므로 시판 후/장기는 짜임과 파일 이름으로, 내수/수출은 파일 이름('수출')과 전년도 결재본의 Lot(앞 두 글자)로 가른다. 작성 기록에 '시점 어림' 근거를 남긴다 | `handwriting.read_pages/read_log` |
| **내수용·수출용이 따로인 서식(퀴노비드)은 13.1.x 장기 / 13.2.x 시판 후 / 13.3.x 경향 표를 제목으로 골라 시장·구분별로 채운다** (담당자 2026-09-06: "안정성이 36개월까지 2025년도에 시험했는데 경향 엑셀 파일에 반영이 안 됐어") | 시판 후 표의 비고는 사용기한(제조일자~사용기한)에 닿았으면 '완료', 아니면 '진행중'. 13.3 은 '시판 후' 줄(제조 연도)과 '장기' 줄을 세로 병합 라벨로 세운다. 성분 이름 없는 '함량(%)' 한 열이면 COA 규격의 성분 하나를 붙인다. 포장 형태는 전년도 결재본 표기('5g tube/갑')를 따른다. 경향 엑셀은 시장마다 한 파일('(내수용)'·'(수출용)') | `recipe_ointment._stability_tables/_fill_131_table/_fill_133_table/_fill_stability26`, `carry.stability_packs`, `excel_attach._write_trend` |
| **빈 공양식의 10.2~10.5 에 관리번호·설비명이 없으면 전년도 결재본의 설비 줄을 세우고 마스터파일로 갱신한다; 10.1 은 전년도 같은 자리 표의 PV 번호(내수 QUIO3·수출 QUIO2)로 마스터를 찾고, 못 찾으면 그 표를 옮긴다** (담당자 2026-09-06: "작성할 줄 모르겠으면 16항의 전년도 PQR 결재본을 참고해서 작성하고 업로드한 파일로 최신 내용으로 업데이트하면 돼") | 퀴노비드 PC 결과: 10.1.1·10.1.2·10.3~10.5 가 비어 있었다 | `recipe_ointment._seed_equipment_rows`, `fill_pv(hint)`, `carry.equipment_rows/section_grids_all` |
| **3항 대상 제품 표의 비고에 'N/A'·'-' 만 적힌 칸은 지우고 빈 줄과 합쳐 사선 하나** (담당자 2026-09-06: "3항 표의 N/A 는 삭제해 주고 아래 칸 사선으로 셀 병합해 줘, 모든 PQR 작성 시 동일하게") | '<주성분>'·'위/수탁품목에 한함.' 처럼 글이 있는 칸은 그대로 두고 그 위·아래 빈 줄끼리만 묶는다 | `docedit.merge_empty_runs` |
| **6항 제조단위·포장단위가 빈 공양식이면 전년도 결재본 같은 표(6.1 내수·6.2 수출)의 값으로 채운다** (담당자 2026-09-06: "제조단위와 포장단위가 공란인 이유가 있나?") | 해마다 같은 값('204,000g'·'5gⅹ1Tube/Case'). 전년도 표에서 가장 많이 적힌 값을 쓰고 작성 기록에 남긴다 | `recipe_ointment.fill_mfg`, `writer` prev_sections_all["6"] |
| **7항 비고: 기준을 벗어난 Lot 은 모두 '1)'**, 각주 한 줄 "1) 충전 수율 일탈로 11항 일탈 관련 기록(DR-…) 참고. (수율의 최대, 최소, 평균 계산에서 제외함.)" (담당자 2026-09-06: "비고에는 1), 2) 가 아니고 모두 1) 로 작성되어야 표 하단 문구로 설명되는 것 아닌지") | 11항에 일탈 기록이 없는 Lot 의 '1)' 은 노랑으로 두고 문의 목록에 적는다. 공양식 각주('1) 충전 수율 일탈로 …')도 찾아 바꾼다 | `recipe_ointment` 7항 |
| **8.1.1·8.1.2(·8.1.3)가 빈 공양식이면 전년도 결재본의 줄(관리번호·원/자재명·규격·제조원…)을 머리행 이름으로 맞춰 세우고, 올린 공급업체 목록·공급망 마스터로 문서번호·완료일·업체를 갱신한다** (담당자 2026-09-06: "주원료명 모르면 작년 PQR 에서 가져오고 해당 항 첨부파일로 최신 정보를 가져오면 돼") | | `recipe_ointment._seed_rows_by_header`, `writer` prev_sections_all |
| **9항은 수출 Lot 이 있어도 머리글 기반으로 채운다** — 9.2.1 내수용 / 9.2.2 수출용 표를 제목으로 갈라 각각, 9.1 도 내수·수출 표를 각각 (2026-09-06 퀴노비드: 자리 기반이면 열이 밀려 확인·질량 칸이 비거나 옆 칸에 들어갔다) | 확인 시험 결과는 올해 성적서가 모두 '확인시험 적합' 일 때 전년도 결재본의 같은 기준 줄 문안, 없으면 기준 문장의 결과형('…나타냄'). 튜브개봉은 성적서에 있는 Lot 만(변경관리로 더해진 항목 — 없는 Lot 은 사선). 튜브인쇄는 성적서 글. 9.1 결과 칸 왼쪽의 빈 쪽칸은 합친다. 시험 결과는 언제나 올해 첨부(성적서)에서, 글 결과 문안만 전년도 참고(담당자: "시험결과도 해당 항 첨부파일에서 최신 정보를 가져오고") | `detail92.tables_92_by_market`, `recipe_ointment.fill_92_labelled/fill_91_labelled/_ident_result/_as_result/_prior_91`, `readers.coa` ident_ok/tube_open |
| **9.2 요약 줄(최댓값·최솟값·평균)은 값이 모두 같아도 적는다**, 공양식의 각주 '모든 시험 결과값이 0매로 동일하여 별도 계산하지 않음' 은 지운다 (담당자 2026-09-06: "최댓값, 최솟값 기재가 안 됐네"; 퀴노비드 2025 결재본도 1/0/1·0/0/0 을 적었다) | 9.1 금속성이물 결과가 합계·개개 두 줄로 갈라진 공양식은 아래 빈 줄을 '개개' 줄로 보고 채운다 | `detail92.fill`, `recipe_ointment` 9.2 |
| **9.2 공양식이 Cpk 칸에 사선을 그어 둔 열(입자도 등)에는 Cpk·판정을 적지 않는다** (담당자 2026-09-06: "Cpk 계산란에 사선이 그어져 있을 경우 값을 기재하면 안 돼") | 계산은 그대로 하되 표에만 적지 않는다(첨부 Cpk 파일·결론은 그대로) | `detail92.fill` |
| **쪽 나눔으로 두 표에 갈라 둔 표(공양식 11.1: 1~3번 표 / 4~5번·특이사항 표)는 하나로 이어 채운다**, '특이사항 (Comment)' 줄은 언제나 한 칸(가로 병합·사선 없음) (담당자 2026-09-06: "일탈 마지막 줄 작성이 잘못됐고") | 뒤 표가 앞 표와 같은 머리행으로 시작하면 이어진 표다. 앞 표만 보고 데이터 줄에 특이사항을 적으면 CAPA 칸·사선이 남는다. 11·12·14·15항 모두 | `docedit.join_continuations/comment_cell` |
| **13항: 올해 시험일지를 읽지 못한 Lot 은 빈칸으로 두지 않는다** — ① 전년도 결재본 13항에서 올해도 이어지는 시험(장기는 마지막 시점 36M 전, 시판 후는 비고 '진행중')의 Lot 을 옮기고 ② 공양식 13.3 특이사항 각주('1) OEX101 … 4) OEY301')에 담당자가 적어 둔 새 Lot 은 줄만 세운다. 시험 기간·완료 일자는 '확인 필요' 노랑, 13.3 은 전년도 범위(노랑)/'확인 필요', 실시 사유는 전년도·10.1 PV 사유, 포장·보관은 전년도 표기. 문의 목록에 어떤 Lot 을 어디서 옮겼는지 적는다 (담당자 2026-09-06: "안정성 자료가 모두 입력이 안 됐네 — 잘 모르겠으면 작년 PQR 에서 정보를 가져오고 첨부된 안정성 자료로 값을 입력") | 값을 지어내지 않는다 — 전년도 결재본 글자와 서식 각주만 옮기고, 올해 시험일지가 올라오면 그 값이 우선. 13.3 연도 번호(1)~6))는 서식 각주 번호를 그대로 쓴다. 13.3 줄 차례는 서식대로(공양식은 장기 먼저) | `carry.stability_entries`, `recipe_ointment._declared_lots/_fill_stability26/_fill_131_table/_fill_133_table` |
| **13.1 표의 해당 연도·제조 번호 열은 서식 폭보다 좁히지 않는다** (2026-09-06: '2024'·'OEWN01' 이 두 줄로 갈렸다) | 열 폭 균등 배분에서 번호·연도 열의 바닥 폭 = 서식 폭·글 길이×1.2 | `docedit.balance_columns NOWRAP_HEADS` |
| **6항에 올린 공(빈) 기록서 워드 — 제조기록서(MO)·충전기록서(FI)·포장기록서(PK) — 를 모든 PQR 에서 참고한다** (담당자 2026-09-06: "제조단위와 포장단위, 수율 기준, 주원료(원료코드가 R 로 시작하면 주원료 / 그 외는 부원료), 부원료, 포장자재 정보 등은 여기 업로드된 공 기록서를 참고하면 돼") | 6항 제조단위·포장단위가 공양식에 비어 있으면 기록서 값 → 없으면 전년도 결재본. 7항 수율 기준은 수율현황표 → 결재본 기준 줄 → 기록서 차례(빈 칸만). 8.1.1 은 R 코드(주원료), 8.1.3 은 그 밖의 원료(부원료)·P 코드(포장자재) — 표에 없는 코드만 줄을 보태고(원/자재명 노랑) 제조원·평가문서는 공급업체 목록으로 채운다. '(수출용)' 이 붙은 기록서는 6.2·7.2 에만. 대시보드 (b) 항목 이름은 '6. 제조 내역 · ERP 및 제조/충전/포장 공 기록서 word' | `readers/batch_record.py`, `collect` 6항, `recipe_ointment fill_mfg/put_sheet_specs/_add_material_rows`, `data/config.json` |
| **빈 쪽·쪽 맨 위의 빈 줄을 만들지 않는다** (담당자 2026-09-06: "22page 공페이지네", "대부분이 맨 위에서 한 칸씩 떨어져 있네 — 백스페이스로 눌러서 조치") | 하드 쪽나눔(br) 문단과 그 뒤에 이어지는 빈 문단은 모두 지우고 다음 글 문단(표면 표의 첫 문단)의 '앞에서 쪽 나눔' 으로 바꾼다. '앞에서 쪽 나눔' 문단 앞의 빈 문단도 지운다 — 표가 쪽을 꽉 채우면 그 빈 문단이 다음 쪽으로 밀려 빈 쪽이 되고, br 문단의 문단 표시가 새 쪽 첫 줄이 된다 | `docedit.hard_breaks_to_page_break_before/strip_blanks_before_page_breaks/tidy_page_breaks` |
| **13항 판독 파일(`13. 안정성시험일지 판독.json`)이 있어도 거기에 없는 시험일지 PDF 는 새로 읽는다** (담당자 2026-09-06: "시험 기간은 시험일지 보면 3개월차면 3M … 완료 일자는 2025년까지 완료된 시험 일자 — 수출용도 마찬가지") | 판독 결과는 읽은 일지 이름(source)을 들고 있어, 뒤에 올린 장기·수출용 일지만 골라 읽고 판독 파일에 보탠다. 시판 후 일지만 읽은 판독 파일 때문에 장기 일지가 통째로 안 읽혀 13.1 이 '확인 필요' 로 남았다 | `collect.unread_scans`, 13항 판독 |
| **14.2 불만 현황표에 불만이 없으면 한 줄**(연번 1 + 사선), 서식이 빈 줄 셋을 세로 병합해 두었어도 한 줄로 줄인다 (담당자 2026-09-06) | 14·15항 표 공통: 글 없는 줄이 여럿이면 하나만 남기고 병합·사선을 푼다 | `docedit.single_blank_row`, `recipe_ointment` 14·15항 |
| **16항 결론 16.1~16.4 는 굵게 쓰지 않고, 번호 폭만큼 내어쓴다**(둘째 줄부터 글이 번호 뒤에 맞춰진다) (담당자 2026-09-06 스샷) | 본문 왼쪽 2글자 들여쓰기에 '16.1 ' 5글자 내어쓰기(leftChars 700 / hangingChars 500) | `conclusion.apply`, `docedit.set_section_indent` |
| **10 Lot 이상이면 Cpk 계산 파일은 어떤 PC 에서든 반드시 만들어진다** (담당자 2026-09-06: "퀴노비드는 10 Lot 이상이라서 이 폴더에 Cpk 도 작성이 되었어야 하는데 안 됐네 — 작년 파일과 올해 PQR 본문 참고해서 작성, 16항 압축 파일 참고") | 16항 압축의 전년도 Cpk .xls 를 서식으로 Excel(COM) → LibreOffice → .xlsx 변환 차례로 채우고, 셋 다 막히거나 전년도 파일이 없으면 프로그램이 서식(HLF-QC-126-08 한쪽/-09 양쪽)의 칸·수식·그래프를 openpyxl 로 직접 그린다. Excel 연결 오류가 그대로 새어 파일이 아예 없던 것을 고쳤다. 파일 이름은 늘 'a. <항목> Cpk 계산 파일.xls(x)', 왜 어느 길로 갔는지 작성 기록·문의 목록에 남긴다 | `excel_attach.write_cpk_files`, `cpk_xlsx.build` |
| **Cpk 계산 파일은 전년도 양식 그대로 만들고 올해 값으로 갱신한다** (담당자 2026-09-06: "Cpk 는 전년도 양식으로 작성하되 2026년 PQR 작성본 내용을 참고해서 업데이트하면 돼") | ① Excel·LibreOffice 가 있으면 전년도 .xls 를 그대로 채우고, ② 없으면 .xlsx 로 바꿔 **칸 값만 갈아 끼운다**(그래프·한림 로고·수식이 그대로 남는다), ③ 전년도 파일이 없으면 프로그램이 지닌 같은 빈 서식(`pqr/data/HLF-QC-126-08·-09`), ④ 그것도 없으면 서식을 직접 그린다. 수식만 적으면 Excel '제한된 보기' 에서 σ·평균·Cpk 칸이 비어 보이므로 **계산값을 수식과 함께 넣어 둔다**. 규격(N6·O6·P6)은 올해 값이 있으면 그것, 없으면 서식에 적힌 값 그대로. 모든 결과가 같아 σ=0 이면 Cpk·판정 칸을 비운다(지난해 값이 남지 않게) | `excel_attach.write_cpk_files`, `cpk_form.fill`, `cpk_xlsx` |
| **12항에 올린 변경요청서는 하나도 빠뜨리지 않는다** (담당자 2026-09-06: "12항 변경관리가 5개인데 1개만 표시되어 있네") | 글자 없는 스캔本이라 읽지 못한 것도 파일 이름의 문서번호(CC-YYMMDD-NN)로 줄을 세우고 변경사항 칸을 '확인 필요' 노랑으로 둔다. 같은 문서번호가 두 번 올라오면 한 줄만 | `collect` 12항, `recipe_ointment` 12항 |
| **내역이 없어 한 줄로 줄인 14·15항 표의 사선은 한 줄을 가로지르는 선 하나** (담당자 2026-09-06: "31쪽 불만 사선도 수정이 안 됐어") | 칸마다 사선을 긋지 않고, 서식이 14.1·14.3·15항에 그어 둔 그리기 개체를 그대로 옮겨 온다 | `docedit.copy_diag_line`, `recipe_ointment` 14·15항 |
| **16항 결론의 내어쓰기 폭은 전각 한 칸이 1** — '16.1 ' 은 반각 5자이므로 2.5칸 (담당자 2026-09-06: "너무 들여쓰기가 됐네") | 반각을 1칸으로 세면 두 배로 밀린다. 왼쪽 2칸 + 번호 폭만큼 내어쓰기(leftChars 450 / hangingChars 250) | `docedit.set_section_indent` |
| **13항 시험일지는 번호 붙은 폴더(`13. 안정성 시험`)에 넣어도 되고, 같은 Lot 이 여러 장·여러 파일에 걸쳐 있어도 한 줄로 합친다** (2026-09-07) | 판독 파일에 없는 일지만 새로 읽고, 같은 Lot 은 시점(3M·6M…)을 맞춰 합친다 — 이미 있는 값(담당자가 고친 판독 파일)이 우선. 구분(장기·시판 후)을 시점 짜임새로 어림한 일지는 이미 읽은 같은 Lot 의 구분을 따르고, 그래도 한 Lot 이 두 구분으로 갈리면 문의 목록에 적어 파일 이름을 고치게 한다 | `handwriting.merge_logs/kind_sure`, `collect` 13항 |
| **13항 손글씨 시험일지는 Claude 로 읽는다 — API 키가 없을 때만 이 PC 가 읽는다** (담당자 2026-09-07: "PC 로 판독하면 시간이 너무 오래 걸려 — Claude 로 판독하면 안 돼?" 24장에 40분) | 프로그램 폴더의 `API-KEY.txt`(또는 `ANTHROPIC_API_KEY`)가 있으면 Claude 가 쪽마다 읽는다(한 번에 4장, 24장이 2~3분). 판독 결과는 PC 판독과 **완전히 같은 꼴**이라 판독 파일(json)·13.1·13.3·경향 엑셀이 그대로 이어진다. Claude 가 막히면 PC 판독으로 넘어간다. 확신이 0.7 밑인 칸은 '애매' 로 남겨 워드 노랑·엑셀 주황으로 표시하고, 값을 지어내지 않는다. 시험일지 그림이 회사 밖(Anthropic)으로 나가므로 키를 두기 전에 회사 방침을 확인한다 | `vision_claude.read_logs/to_log`, `collect` 13항, `PQR-대시보드-실행.bat` |
| **API 키 없이도 13항을 채울 수 있다 — 시험일지를 이 대화에 올리면 Claude 가 읽어 판독 파일(json)로 돌려준다** (담당자 2026-09-07: "API 키를 발급받지 않고 Claude Opus 5 나 Fable 5.1 로 확인할 수는 없나?") | 판독 파일에 `"covers_all": true` 를 적어 두면 프로그램이 13 폴더 PDF 를 다시 읽지 않는다(PC 판독 40분을 건너뛴다). 파일 이름은 `13. 안정성시험일지 판독.json`, 자리는 제품 폴더. 담당자가 값을 고치면 그 값이 언제나 우선이고, 애매한 칸은 `unsure` 에 남겨 노랑·주황으로 표시한다 | `collect` 13항(covers_all), `handwriting.save_cache` |
| **13항 손글씨 판독의 기본은 'Claude 대화' 다 — 이 PC 로는 읽지 않는다** (담당자 2026-09-07: "PC 판독하지 말고 Claude 에서 확인해 주도록 해", 20장에 71분) | 보고서를 만들 때 판독이 필요하면 제품 폴더에 `13. Claude 판독 요청 …zip`(시험일지 + 읽어주세요.txt + json 서식)을 만들고 보고서는 바로 낸다. 담당자가 그 zip 을 대화에 올려 받은 `13. 안정성시험일지 판독.json`(covers_all)을 폴더에 두고 재작성하면 13항이 채워진다. `API-KEY.txt` 가 있으면 Claude 가 곧바로 읽고, 제품 폴더에 `PC 판독 사용.txt` 를 두면 예전처럼 이 PC 가 읽는다. 쪽 병렬 판독은 효과가 없다(측정: 4장 30초 → 45초, onnxruntime 이 이미 코어를 다 쓴다) | `handreq.make_request/pc_reading_on`, `collect` 13항 |
| **이 PC 에 Claude Code 가 깔려 있으면 '보고서 작성' 이 그것으로 13항 손글씨를 읽는다 — API 키도, 담당자가 따로 물어볼 일도 없다** (담당자 2026-09-07: "매번 명령하라는 거야? 자동화하면 안 돼?") | `claude -p` 를 읽기(Read)만 허용하고 명령 실행 도구 없이(`--restricted`) 부른다. 한 번에 6장씩 물어 답(JSON)만 받아 PC 판독과 같은 꼴로 다듬는다. 차례는 ① API 키 → ② 이 PC 의 Claude Code → ③ 'PC 판독 사용.txt' 가 있으면 RapidOCR → ④ 셋 다 없으면 대화에 올릴 zip 묶음. 한 장에 1~2분이라 스무 장이면 십수 분 | `claude_cli.read_logs`, `collect` 13항, `server` handwriting_reader |
| **13항 시험일지는 같은 것을 두 번 읽지 않는다** (담당자 PC 2026-09-07: 13 폴더 24장 가운데 실제 문서는 여덟 가지, 게다가 옛 판독 파일 보완으로 한 번 + 새로 한 번 = 44번 읽어 한 시간이 넘었다) | ① 내용(md5)이 같은 파일은 하나만 읽는다 — 담당자가 같은 일지를 이름만 바꿔 여러 번 올려 둔다. ② 새로 읽을 일지가 있으면 옛 판독 파일 보완(refresh_cache)은 건너뛴다 — 새로 읽으면서 어차피 채워진다. ③ 판독 기록에 `kind` 가 없어도 넘어지지 않게 글자로 맞춘다(옛 판독 파일이 섞이면 sorted 가 None 과 글자를 견주다 멈췄다) | `collect.same_files_once`, `collect` 13항 |
| **갈음(대신하는) 제품의 시험일지도 그 제품의 것으로 읽는다** (담당자 2026-09-07: "수출용을 에펙신으로 갈음한 거야 — 동일한 충전량이고 에펙신은 퀴노비드와 동일한 제품, 일동제약 수탁품이야") | 갈음 제품 이름은 서식이 스스로 알려 준다 — 13.1.2·13.2.2 특이사항의 '동일 수탁 제품(에펙신안연고)로 갈음하였음' 에서 읽어, 그 이름이 든 시험일지(`[LT-24-2]에펙신안연고_OAX101(MTC001)_24M.pdf`)를 그 표(수출용)의 것으로 본다. 제품마다 갈음 제품이 다르므로 이름을 코드에 박지 않는다. 파일 이름에 '수출'·'내수' 가 있으면 그것이 먼저고, 그다음이 갈음 이름, 그다음이 전년도 결재본의 제조번호 앞 두 글자. 서식의 갈음 문구는 지우지 않는다 | `recipe_ointment._stand_in_names/_fill_stability26` |
| **공 기록서에서 8.1.3 에 보태는 것은 원/자재만** — 저울 같은 계측기·설비(FAB… 코드)와 배송용 '하조용 종이상자' 는 넣지 않고, 표의 관리번호가 기록서와 한 자만 다르고 이름이 같으면 결재본 오기로 보고 기록서 번호로 고친다(줄을 보태지 않는다) (담당자 2026-09-07: "전자 저울과 하조용 종이상자는 지워 주고, 케이스는 내수 케이스야 — 기존 P38033 관리번호 오기라서 P38003 으로 수정해 줘") | 고친 관리번호는 노랑으로 두고 문의 목록에 '옛 → 새' 로 남긴다. 이름이 같은 줄이 여럿이면(케이스 내수·수출) 관리번호가 한 자 차이인 줄만 고친다 | `readers/batch_record.is_material`, `recipe_ointment._add_material_rows` |
| **16항 결론은 단락 들여쓰기 왼쪽 2글자, 16.1 아래 Cpk 표는 그 본문 폭을 벗어나지 않는다** (담당자 2026-09-07: "16.1~16.4 는 단락 들여쓰기 왼쪽 2글자로 변경해 줘 / 16.1항 아래 있는 표 크기는 16.1항을 벗어나면 안 돼") | 왼쪽 들여쓰기 2칸(leftChars 200)에 번호만 그만큼 내어쓴다(hangingChars ≤ 2칸 — 첫 줄이 여백 밖으로 나가지 않게). 16.x 문단 바로 아래 표는 그 문단의 왼쪽 들여쓰기만큼 들여 놓고(tblInd) 폭을 '본문 폭 − 들여쓰기' 로 줄인다 — 이 손질은 '창에 자동으로 맞춤' 뒤에 해야 다시 넓어지지 않는다 | `docedit.set_section_indent/inset_table/para_left_before`, `layout` |
| **Windows 의 `claude.CMD` 는 한 겹 건너 부르고, 물음은 파일로 넘긴다** (담당자 PC 2026-09-07: 판독이 `input_tokens 0 · duration_api_ms 0` 으로 끝났다 — 물음이 claude 에 닿지 않았다) | `.cmd`·`.bat` 이면 옆의 `node_modules/@anthropic-ai/claude-code/cli.js` 를 `node` 로 바로 부른다(cmd.exe 겹이 없어진다). 물음은 파이프 대신 임시 파일을 표준 입력으로 물려 준다. 실패하면 겉봉투의 `result` 를 그대로 보여 준다 — 이 두 가지가 판독·설치 확인에 모두 쓰인다 | `claude_cli.command/ask_once`, `cli._check_login` |
| **로그인 창의 차례를 그대로 적어 준다 — 스타일 고르기 → 로그인 방법 → 브라우저 → /exit** (담당자 2026-09-07: "로그인 방법 고르라고 안 나오는데" — 첫 화면이 글자 스타일 고르기라 헷갈렸다) | 'Claude account with subscription'(구독)을 고르게 하고, 'Anthropic Console (API key)' 는 고르지 말라고 짚어 준다. 붙여넣기는 마우스 오른쪽 클릭 | `cli.cmd_login_claude` |
| **로그인은 두 번 클릭 파일 하나로 — `PQR-Claude로그인.bat`** (담당자 2026-09-07: "claude 치려고 하면 화면이 닫혀", "이 창에서 claude 를 치면 창이 닫아지는데 어떻게 해?") | 설치 파일은 끝나면서 창을 닫으므로 그 창에서는 `claude` 를 칠 수 없다. 로그인만 하는 파일을 따로 두고, `pqr login-claude` 가 claude 를 **이 창에 대화형으로** 띄운 뒤(콘솔을 그대로 물려 준다) 로그인이 됐는지까지 확인한다. 설치 도우미도 '깔렸지만 로그인 전'(종료 코드 2)이면 그 창을 대신 띄운다 | `cli.cmd_login_claude`, `PQR-Claude로그인.bat`, `PQR-Claude설치.bat` |
| **Claude Code 설치도 두 번 클릭으로 — `PQR-Claude설치.bat`** (담당자 2026-09-07: "설치하려면 어떻게 해야 돼?") | Node.js(npm)가 없으면 어디서 무엇을 받아야 하는지 알려 주고, 있으면 `npm install -g @anthropic-ai/claude-code` 를 대신 돌린 뒤 찾은 경로를 `CLAUDE 경로.txt` 에 적어 둔다. 마지막 로그인(`claude` 한 번 실행)만 담당자가 한다. 배치 파일은 ASCII 만, 한글은 파이썬이 찍는다 | `cli.cmd_install_claude`, `PQR-Claude설치.bat` |
| **`claude` 는 PATH 만 보지 않는다 — 설치 자리를 두루 찾고, 못 찾으면 어디를 뒤졌는지 알려 준다** (담당자 2026-09-07: "이 PC 로 Claude Code 로 작업하고 있는데 없다는 게 무슨 말이냐") | 브라우저·Claude 앱 화면과 이 PC 의 `claude` 명령은 다르다. 찾는 자리: PATH → `%USERPROFILE%\.local\bin` → `%APPDATA%\npm` → `%LOCALAPPDATA%\Programs\claude` → `~/.claude/local` 등. 담당자가 `where claude` 로 확인한 경로를 프로그램 폴더의 `CLAUDE 경로.txt`(또는 환경변수 `PQR_CLAUDE_EXE`)에 적어 두면 그것을 쓴다. 새로 깔았다면 창을 다시 열어야 PATH 가 반영된다 — `PQR-진단.bat` 이 이 모든 것을 짚어 준다 | `claude_cli._exe/places/HINT_FILE`, `doctor.report` |
| **쪽 맨 위에 홀로 남은 빈 줄은 Word 가 쪽을 나눈 뒤에 지운다** (담당자 2026-09-07: "이렇게 오른쪽처럼 del 키 눌러서 빈 공간 없게 만들어 줘") | 쪽이 어디서 나뉘는지는 Word 만 안다 — 목차 쪽수를 다시 계산할 때(refresh_fields) 함께 지우고 다시 쪽을 나눈다. 표 안의 문단과 **표와 표 사이의 빈 문단은 건드리지 않는다**(지우면 두 표가 하나로 붙는다) | `convert.TOP_BLANKS_VBS/_PS/_drop_top_blanks` |
| **13.3 특이사항에 '어느 해가 어느 Lot 인지' 를 적는다** (담당자 2026-09-07: "이 정보가 13.3.1 특이사항 칸에 기재되어야지") | 경향표에는 연도만 적혀 2024¹⁾·2024²⁾ 가 어느 제조번호인지 표에서 알 수 없다. 특이사항에 `* 해당 연도의 제조번호 — 장기: 2023 OEWN01, 2024 1) OEX101 … / 시판 후: 2022 OEV301 …` 한 줄을 적고, 서식에 담당자가 적어 둔 다른 글(갈음 문구 등)은 그대로 둔다 | `recipe_ointment._note_133_lots` |
| **첨부가 이번 작성에서 새로 쓰였는지 확인하고, 예전 첨부가 남아 있으면 ★ 로 알린다** (담당자 2026-09-07: "다시는 실수하지 않게 조치해 줘") | 파일 시각이 이번 실행보다 오래됐으면 '엑셀에서 열려 있는지 확인하고 닫은 뒤 다시 작성하세요', 이번에 만들지 않은 같은 종류의 첨부가 폴더에 있으면 '본문과 값이 다를 수 있으니 지우거나 확인하세요' 를 문의 목록 맨 앞에 올린다 | `writer._check_attachments` |
| **첨부 파일을 덮어쓰지 못하면(엑셀에서 열려 있으면) 다른 이름으로 만들고 ★ 로 알린다** (담당자 2026-09-07: "안정성 Lot No 가 워드 파일과 엑셀 파일이 상이해 — OEY301·302 가 없네") | 조용히 지나가면 예전 첨부가 그대로 남아 본문과 어긋난다. `… (새로 만든 것).xlsx` 로 만들고 문의 목록 맨 앞에 이유를 적는다 — 엑셀을 닫고 재작성하면 원래 이름으로 만들어진다 | `excel_attach.free_path` |
| **Cpk 파일의 범례는 그래프 밖 오른쪽, 사선은 빈 칸을 남김없이 덮는다** (담당자 2026-09-07: "Cpk 범례표가 그래프를 가리네", "사선은 빈 공간을 모두 커버해야 돼 — 함량 Cpk 도 마찬가지") | 옛 .xls 를 .xlsx 로 바꾸면 범례에 '손으로 정한 자리'(manualLayout)가 붙어 그래프 한가운데에 얹히고, 그림 영역이 판을 꽉 채워(w 0.96) 범례 자리가 없다 — 둘 다 지우면 `legendPos="r"` 대로 엑셀이 바깥에 놓는다. 사선(결과값 열의 직선 도형)은 '값이 끝난 다음 줄 ~ 마지막 줄(44)' 로 자리를 다시 잡고, 빈 줄이 없으면 지운다. 한쪽 규격(-08)·양쪽 규격(-09) 두 서식 모두 | `cpk_form._legend_outside/_patch_drawing` |
| **'확인 필요' 노랑 표시는 그 낱말로 시작하는 줄에만** (담당자 2026-09-07: "본문에 노랑 마크는 왜 한 거지?" — 12항 변경사항의 '1.3 … 교정 주기 설정 → QA팀 확인 필요' 가 칠해졌다) | 자료에서 옮겨 온 글에 그 낱말이 들어 있을 수 있다. 프로그램이 적는 자리는 언제나 그 낱말로 시작하므로('확인 필요', '확인 필요 — 변경요청서를 읽지 못했습니다') 줄 첫머리로만 가른다 | `docedit.highlight` |
| **공양식이 바탕이면 전년도 결재본의 표 뼈대를 먼저 옮겨 심는다 — 4·9.1·9.2.1~9.2.4·10.2·13.3** (담당자 PC 2026-09-07 한림포비돈점안액: "도대체 왜 작성이 안 되는 거야?", "너가 이 보고서 전체를 만들어 내야 돼, 다른 제품 만들 때도 이러면 안 돼") | (v) 0항의 빈 공양식이 바탕이 되면 9.1 시험항목·허용기준, 9.2 열 머리글, 10.2 라인별 설비 목록, 13.3 시험항목 머리글, 4항 문안이 통째로 없어 전부 사선이 된다. 새 양식(표지·항 구성·문안)은 그대로 쓰되, 그 항이 빈 뼈대뿐이면 전년도의 그 항(표·소제목·문단)을 옮겨 심고 나서 채운다. 옮길 때 전년도 **값은 비운다**(9.1 결과 열, 9.2 Lot 줄·요약 줄) — 작년 숫자가 올해 칸에 보이면 안 된다. 쪽 나눔으로 둘로 갈라진 9.1 표는 하나로 합친다(둘째 표를 수출용으로 오해하지 않게). 공양식에만 있는 '※ Cpk 판정표' 블록은 남긴다. 값이 이미 든 항은 건드리지 않는다 | `carry.adopt_skeleton`, `writer` 의 product_form 갈래 |
| **8.2.1.x 원료명은 표에 없으면 소제목('8.2.1.1 포비돈 K-25')에서 가져온다** (담당자 2026-09-07: "원료명에 왜 사선이야, 포비돈 K-25 라고 기재하면 되잖아") | ERP 원료 시험표에는 코드(RGP109)만 있고 한글 이름은 결재본 표나 소제목에만 있다. 결재본 표에 그 코드가 없으면 그 표 바로 앞 소제목의 이름을 쓴다 | `recipe_ointment._heading_material_name` |
| **다른 제품의 공 기록서는 쓰지 않고 ★ 로 알린다** (담당자 2026-09-07: "10mL 생산했으니까 5mL 관련 정보는 기재하지 않는 거야" — 한림포비돈점안액 폴더에 '올로원스점안액 3ml' 기록서가 있어 PE병 5mL 등 남의 자재가 8.1.3 에 들어갔다) | 6항 공 기록서 파일 이름에 이 제품 이름은 없고 다른 '…점안액/…안연고' 이름이 있으면 그 제품 것으로 보고 제조단위·포장단위·자재를 가져오지 않는다. 이름이 아예 없는 기록서(제조기록서 rev3)는 쓴다 | `collect.other_product_record` |
| **변경요청서를 읽었는데 문서번호가 비면 파일 이름의 CC 번호를 쓰고, 제목도 조치도 없으면 '못 읽음' 으로** (담당자 2026-09-07: "변경관리 내용도 기재 안 했네" — 12항에 '[None]' 이 나갔다) | '[None]' 이 나가면 안 된다. 파일 이름 '12.CC-240723-08.pdf' 의 번호를 적고, 노랑 '확인 필요' 로 남긴 뒤 문의 목록에 올린다 | `collect` 12항 |
| **7항 수율표의 조제·충전·포장(내수)·포장(수출) 네 칸은 같은 폭** (담당자 2026-09-07: "수율현황표는 칸 크기를 동일하게 해 줘") | 네 열의 폭 합은 그대로 두고 똑같이 나눈다(앞글자 '포장' 으로 맞춰 포장(베트남)도 든다). 연번·공정·비고 폭은 그대로 | `docedit.equal_columns(prefix=True)`, `layout` 7항 |
| **제목 바로 뒤 본문에 붙은 '앞에서 쪽 나눔' 은 뗀다** (담당자 2026-09-07: "16항 결론은 왜 다음 페이지로 밀렸는지? 21페이지에 작성되도록 해") | '16. 결론' 제목이 쪽 맨 위에 홀로 남고 본문이 다음 쪽으로 갔다 — 본문 문단이 쪽 나눔을 물려받은 탓. 제목 뒤(빈 문단 건너) 첫 본문 문단의 pageBreakBefore 를 뗀다. 제목이 아닌 자리의 쪽 나눔은 그대로 | `docedit.drop_break_after_headings` |
| **안정성 경향 그래프의 세로축은 그 시험항목의 관리 규격에 맞추고, pH·보존제는 제 이름의 시트를 쓴다** (담당자 2026-09-07: "안정성도 그래프가 안 그려졌어" — 시트 '함량(pH)' 의 축이 90~110 이라 pH 7.4 가 축 밖으로 나갔다) | 축은 규격 위아래로 폭의 1/4 씩 여유(6.0~8.0 → 5.5~8.5). 규격이 없으면 엑셀에 맡긴다. 시트는 서식에 같은 이름(pH·보존제·삼투압)이 있으면 그것을, 함량 성분은 '함량·A·B…' 차례로, 한 시트를 두 번 쓰지 않는다. 시트 이름도 'pH'·'보존제'·'함량(성분)' 으로 | `stability_xlsx.axis_range/_scale_axis`, `excel_attach.pick_form_sheets` |
| **17항 제품표준서 줄이 다른 제품 이름이면 전년도 결재본의 줄로 바꾼다** (담당자 PC 2026-09-07: 한림포비돈점안액 공양식 17항에 '제품표준서 MF-5008 퀴노비드점안액' 이 남아 있었다) | 공양식을 다른 제품 것에서 복사해 만든 흔적. 전년도 결재본에 이 제품 이름이 든 제품표준서 줄이 있으면 그것으로, 없으면 문의 목록에 올린다 | `carry.adopt_references`, `writer._flag_wrong_reference` |
| **8.2.3·11.1·11.2 도 내역이 없으면 연번을 지우고 한 줄 사선** (담당자 2026-09-07: "8.2.3, 11.1 및 11.2는 기재할 내용 없으면 연번 삭제하고 이런 식으로 사선 처리해") | 14.2 불만 표에 하던 것과 같다 — 머리행과 특이사항 사이의 빈 줄 여럿을 한 줄로 줄이고, 연번 '1' 도 지우고, 칸마다 사선을 긋는 대신 서식이 그어 둔 '한 줄을 가로지르는 선' 을 옮겨 온다 | `recipe_ointment` 빈줄표, `docedit.single_blank_row/copy_diag_line` |
| **용량 없는 포장 형태 판독값은 쓰지 않는다** (담당자 2026-09-07: "2022 포장형태가 왜 PE야") | 손글씨에서 'PE' 처럼 용량(숫자+g·mL)이 없는 글이 읽히면 포장 형태가 아니다. 같은 시장에서 가장 많이 쓰인 표기(10mL/BTL)로 바꾸고 문의 목록에 '무엇으로 읽었고 무엇으로 적었는지' 를 남긴다 | `recipe_ointment._fill_stability26` 포장 형태 |
| **특이사항 줄은 복제하지 않는다 — 자료 줄은 '특이사항' 바로 위까지** (담당자 2026-09-07: "특이사항은 연번 4로 적는 게 아니고 13.2항 표 연번 3 아래 바로 기재해야지") | 자료 줄이 모자라면 마지막 자료 줄을 복제해 늘리는데, 그 자리를 '끝에서 두 번째' 로 박아 두면 특이사항 줄이 복제되어 그 글이 연번 줄 안으로 들어간다. '특이사항' 으로 시작하는 줄을 찾아 그 **바로 위**를 마지막 자료 줄로 삼는다 | `recipe_ointment._last_data_row` (13.1·13.2) |
| **일탈·변경·반품·시정조치(11·12·14·15)는 전년도 값을 이어받지 않는다** (담당자 2026-09-07: "일탈이 없는데 일탈 문서번호는 왜 작성한 거야?") | 이어받기는 '해마다 같은 값'(규격·제조원·문서번호…)을 빈 칸에 넣는데, 11.1 의 '문서번호' 도 그 이름이라 전년도 일탈 번호(DR-240509-06)가 옮겨져 '이력 없음' 이라는 특이사항과 어긋났다. 이 네 항은 올해 자료로만 적는다 | `carry.THIS_YEAR_ONLY` |
| **9.2.1 도 조제 단계 성적서로 읽는다** (담당자 2026-09-07: "알집은 못 읽는 거야?" — 압축은 원래 읽는다) | 점안제는 조제 성적서를 9.2.1(점안제)·9.2.2(바이오버든)로 갈라 올린다. 프로그램이 9.2.2~9.2.4 만 읽어 9.2.1 넉 장이 통째로 버려졌다. 9.2.1 을 읽어 9.2.2 에 **없는 값만** 보탠다 — 같은 조제 단계라 한 곳에서 보면 된다 | `collect` 9.2.x 고리 |
| **7항 수율의 공정 열은 표에서 읽는다 — 포장이 시장별로 갈린다** (담당자 2026-09-07: "ELYN01 과 ELYN02 는 내수가 아니고 베트남 포장했네") | 수율현황표의 포장이 '내수'·'베트남(한비돈점안액)' 두 열인데 이름이 둘 다 '포장' 이라 뒤엣것이 앞엣것을 덮었고, 보고서 표의 열도 셋으로 박혀 베트남 열이 빠졌다. 시트는 '포장(내수)'·'포장(베트남)' 으로 읽고, 표의 열은 '공정' 칸 오른쪽부터 '비고' 앞까지 읽는다. 그 시장으로 포장하지 않은 Lot 은 '확인 필요' 가 아니라 **사선**이고, 최댓·최솟·평균도 열마다 따로 센다 | `yield_sheet._split_markets`, `recipe_ointment._yield_columns/_yield_value/_other_market` |
| **좁은 칸은 넓혀 글씨가 세로로 쪼개지지 않게** (담당자 2026-09-07: "칸은 글씨가 1줄이나 2줄에서 모두 나오게") | 8.1.x 의 '규격'·'평가결과' 가 좁아 '자사규격' 이 넉 줄, '적합' 이 두 줄로 세로로 갈라졌다. 글자 두 개가 들어갈 폭(글자 200 + 여백 220 twip)을 못 채우면 가장 넓은 열에서 그만큼 덜어 온다 — 표 전체 폭은 그대로 | `docedit.widen_narrow_columns`, `layout` 8.1.x |
| **경향 엑셀은 성분 이름이 달라도 판독값을 찾는다** (담당자 2026-09-08: 판독 3 Lot 이 있는데 "안정성 시험 결과값이 없어 경향 분석 파일을 만들지 못함") | 경향표는 성분 이름(`트레할로스수화물`)으로 찾는데 시험일지 판독값은 다른 이름(`함량`)으로 저장될 수 있다. 같은 계열 이름을 먼저 찾고, **성분이 하나뿐이면 이름이 달라도 그 값**을 쓴다 — 성분이 여럿이면 짐작하지 않는다 | `excel_attach.points_by_part` |
| **보고서가 워드에서 열려 있으면 다른 이름으로 저장한다** (담당자 PC 2026-09-08: PermissionError 로 작성 전체가 실패) | 만들어 둔 보고서를 워드로 열어 둔 채 재작성을 누르면 `document.save` 가 막혀 **판독까지 헛일**이 된다. 첨부 엑셀과 같이 `… (새로 만든 것).docx` 로 저장하고 문의 목록 맨 앞에 ★ 로 알린다 — 워드를 닫고 다시 만들면 원래 이름으로 간다 | `writer._writable` |
| **'Failed to fetch' 는 사람 말로 바꿔 준다 — 프로그램 창이 닫혔거나 주소가 다른 것** (담당자 2026-09-08: "보고서 작성이 안 됨") | 브라우저가 프로그램에 닿지 못했다는 뜻이지 작성이 실패한 것이 아니다. 화면이 보고 있는 주소(포트)를 적어 주고, ① 검은 프로그램 창이 닫혔는지 ② 창을 여러 번 띄워 주소가 달라졌는지(옛 탭을 보고 있는지) ③ 그 창의 빨간 글을 보내 달라 고 알린다. 서버 쪽도 500 을 낼 때 멈춘 자리를 검은 창에 찍어 둔다 | `index.html netMessage`, `server.do_POST` |
| **'적재 오류 N건' 은 눌러서 내용을 볼 수 있어야 한다** (담당자 2026-09-08: "적재 오류 27건이 무슨 뜻이지?") | 이것은 **대장 엑셀**(제품 마스터·PQR 일정·일탈 대장 …)을 표로 읽을 때 생긴 문제이지, 제품 폴더에 올린 PQR 자료와는 다른 이야기다. 그 줄만 빠지고 나머지는 그대로 쓰인다. 숫자만 보내면 무엇이 잘못됐는지 알 수 없으므로 목록(앞 50건)도 함께 보내고, 배지를 누르면 파일별로 몇 건인지와 줄·열·까닭을 보여 준다 | `server.dashboard_payload issue_list`, `index.html showLoadErrors` |
| **작성 전에 16항 전년도 결재본을 항별로 분석하고, 다 채운 뒤 견준다** (담당자 2026-09-08: "PQR 작성 전에 16. 전년도 PQR 분석을 한 뒤에 작성해 줘야 돼") | 작성에 들어가기 전에 전년도 결재본의 항별 자료 줄 수를 세어 두고(`_analyse_previous`, 작성 기록 맨 앞에 남는다), 다 채운 뒤 같은 항을 다시 세어 **전년도에는 있었는데 올해 빈 항**을 문의 목록 맨 앞에 ★ 로 올린다. 다 만든 뒤 워드를 열어 보고서야 아는 일이 없게 한다 | `writer._analyse_previous/_compare_with_previous` |
| **본문 항 번호 = 첨부 자료 번호 — 그 번호의 자료를 보고 그 항을 쓴다** (담당자 2026-09-08: "7항 수율표를 작성할 때는 첨부문서 7번을 참고하고 나머지도 동일해") | 파일 이름이 항 번호로 시작하면 그 항 자료다(`6. 제조내역`, `7. 수율현황표`, `9.2.1 조제 완료 후`, `10.1 PV 마스터`, `13 안정성 시험일지`). 번호가 붙은 **폴더**와 **압축** 안의 파일도 같은 항으로 친다. 모르면 16항 전년도 결재본의 그 항을 본다. 항마다 무엇을 읽는지는 `collect.READABLE` 한 곳에 적혀 있고, 읽지 못하거나 한 칸도 못 채우면 ★ 로 알린다 | `collect._item_of/discover/READABLE`, `empty_after_read` |
| **7항 표 머리가 여러 줄이면 이어 읽고, 시장이 적힌 열은 그 시장 값만 쓴다** (담당자 2026-09-08: "온누리 값을 잘못 넣었어") | 보고서 표도 수율현황표와 같다 — '포장' 한 줄 아래 '내수·온누리 에이치엔씨' 두 칸으로 갈린다. 가로로 걸친 머리 칸은 **덮는 열 모두**에 같은 글로 펴고(`_row_texts`), 열 이름을 '포장(내수)'·'포장(온누리에이치엔씨)' 로 잇는다. 값을 찾을 때 괄호를 뗀 이름으로만 견주면 온누리 값이 내수 칸에 들어간다 — 시장이 적힌 열은 **그 시장으로만** 찾는다 | `recipe_ointment._row_texts/_yield_columns/_yield_value` |
| **9.2 표의 머리행 아래 빈 줄은 지운다** (담당자 2026-09-08: "1번 연번 위의 쓸모없는 행 2개를 삭제해야지") | 공양식의 9.2 표에는 빈 줄이 끼어 있는데 첫 칸이 비어 있어 머리행으로 셈해져 그대로 남는다. 글이 하나도 없는 줄만 지우고, 글이 있는 머리행('질량·용량 / 평균·개개')은 건드리지 않는다 | `detail92.drop_blank_head` |
| **10.1 PV 는 라인별 시트를 모두 보고, 문서 코드의 밑줄을 살린다** (담당자 2026-09-08: "해당 라인은 충전기 기준 BFS 2호야, 10.1 작성할 때는 첨부 파일 10.1 을 참고해서 PV 25년도까지 최신본을 확인 후 작성") | PV 마스터는 라인별 시트(Mar 1호·Mar 2호·마케지니·BFS 1호·BFS 2호·BFS 3호·위탁)로 갈라져 있는데 첫 시트만 보고 있었다. 그리고 문서 코드에 밑줄이 든다(`PV25-5-TRHE1_1-P`) — 코드 뽑는 규칙에 `_` 가 빠져 한 건도 못 찾고 전년도 10.1 을 옮겼다. 둘을 고치니 2025년 PV(`PV25-5-TRHE1_1-R-1`, Lot LWY501·LWYO01)가 잡힌다 | `masters.pv_by_code`, `recipe_ointment.fill_pv` |
| **9.2 표는 성적서의 시험항목 표를 그대로 보고 채운다** (담당자 2026-09-08: "9.2.1 시험 압축 파일을 참고해서 9.2.1 표를 작성하면 돼, 9.2.2·9.2.3·9.2.4 도 마찬가지") | 항목을 코드에 박아 두면 제품이 바뀔 때마다 빈다(점안제의 pH·삼투압·비중). 성적서의 '시험항목·시험기준·시험결과' 표를 통째로 읽어 두고(`coa.test_items`), 보고서 열 이름과 맞춰 채운다. 9.1 은 그 결과들을 요약한 것이니 9.2 를 먼저 채운다. 모르면 전년도 결재본을 본다 | `coa.test_items`, `recipe_ointment._item_value` |
| **8.2.1·8.2.2 는 ERP 엑셀을 보고 적는다 — 제품명은 느슨하게 견준다** (담당자 2026-09-08: "8.2.1항과 8.2.2항은 첨부 엑셀파일을 보고 적으면 돼") | ERP 표의 제품명은 `아이퓨어점안액[일회용]` 인데 제품 이름은 `아이퓨어점안액` 이다. 정확히 견주면 한 줄도 남지 않아 표가 통째로 사선이 된다. 대괄호·괄호 설명을 떼고 서로의 앞부분이면 같은 제품으로 본다 | `erp._same_product` |
| **8.1.3 에 설비 관리번호를 넣지 않는다** (담당자 2026-09-08: "고압증기 멸균기는 생산 장비야") | 공 기록서의 자재 목록에 설비 번호(DAE5024 …)가 섞여 온다. `DA·DE·FA·HA·HC·HE·AI·CW` 로 시작하는 번호는 설비이므로 8.1.3(부원료·포장자재)에서 뺀다 — 설비는 10.2 가 다룬다 | `recipe_ointment._is_equipment` |
| **7항 자료 첫 줄은 기준 줄 다음** (담당자 2026-09-08: "LWY201 수율 값이 기준에 적혀 있네") | 머리가 세 줄인 표(중요공정 / 공정·조제·충전·포장 / 내수·온누리)가 있어 자료 시작을 넷째 줄로 박아 두면 첫 Lot 이 기준 줄에 찍힌다. 기준(±·이상·이하)·이름(공정·기준·Lot No.) 줄을 찾아 그 다음을 자료 첫 줄로 삼는다 | `recipe_ointment._yield_first_row` |
| **관리번호·품명은 노랑으로 칠하지 않는다** (담당자 2026-09-08: "8.1.1 히아루론산나트륨은 왜 노랑 마크인지?") | 해마다 같은 것이 정상이라 '전년도 값 그대로' 로 칠하면 뜻이 흐려진다. 노랑은 **갱신되지 않은 값**(규격·제조원·평가문서·완료일)에만 쓴다 | `recipe_ointment._mark_carried_cells` |
| **자료는 올렸는데 한 칸도 못 읽은 항은 작성 전에 ★ 로 알린다** (담당자 2026-09-08: "다른 제품 작성할 때 동일한 문제가 발생 안 되도록 조치해 줘") | 형식이 맞는 파일이 있는데도 값이 비면 그 항은 사선으로 남고, 담당자는 다 만든 뒤에야 안다. 항마다 '읽혔다면 여기에 값이 있어야 한다'(`FILLED_BY`)를 정해 두고, 파일은 있는데 그 자리가 비면 문의 목록 맨 앞에 올린다 — 몇 개를 올렸고 무엇을 못 읽었는지, 파일 이름까지. 마감 글(.txt)과 우리가 남긴 글(PQR …)은 세지 않는다 | `collect.empty_after_read` |
| **수율현황표의 머리가 여러 줄이면 위에서 아래로 이어 읽는다** (담당자 2026-09-08: 아이퓨어 세 Lot 모두 '포장 수율 없음') | '조제 | 충전 | 포장 | 포장' 아래에 '조제 | 충전 | 내수 | 온누리에이치엔씨' 가 따로 있는 표가 있다. 가장 아래 줄만 보면 이름이 '내수'·'온누리에이치엔씨' 가 되어 '포장' 이라는 공정을 잃는다. 열마다 위에서 아래로 이름을 이어 '포장(내수)' 로 만들고, 값 열 전체에 같은 글이 퍼진 줄(표 제목)과 숫자가 든 줄(기준)은 이름이 아니다 | `yield_sheet._column_names` |
| **글자 없는 스캔 시험성적서도 Claude 로 읽는다** (담당자 2026-09-08: "시험 성적을 제대로 못 읽는 것 같아") | 아이퓨어점안액의 9.2.4 완제 성적서(LIMS 연동 X)가 세 쪽 모두 글자 0자인 스캔본이라 한 칸도 못 읽었고, 9.1 허용기준(입자도·함량·금속성이물)도 거기서 오므로 함께 비었다. 글자 판독으로 얻은 칸이 하나 이하이고 스캔본이면 Claude(API 키) → 이 PC 의 Claude Code 로 읽는다. 성적서는 Lot 마다 있어 여러 장이므로 `PQR 시험성적서 판독.json` 에 되쓴다 | `collect._coa_by_claude`, `vision_claude.read_coa`, `claude_cli.read_coa` |
| **Word·Excel 을 부르기 전에 그 갈래의 COM 을 연다** (담당자 PC 2026-09-08: "목차 쪽 번호 계산 실패: CoInitialize가 호출되지 않았습니다") | 화면(서버)은 일감마다 새 갈래에서 도는데 COM 은 갈래마다 한 번 열어 주어야 한다. 이것이 빠지면 **목차 쪽수와 '쪽 맨 위 빈 줄 지우기' 가 통째로 건너뛰어진다** — 조판이 하나도 안 된 것처럼 보인다. Word 두 곳·Excel 한 곳 모두 앞에 `_com_ready()` 를 둔다 | `convert._com_ready`, `xls_fill._with_excel` |
| **수율현황표의 병합된 머리 칸은 덮는 열 모두에 같은 값으로 편다** (담당자 2026-09-08: 포장(베트남) 수율이 계속 '없음') | '포장' 머리 칸이 내수·베트남 두 열에 걸쳐 병합돼 있어 베트남 열의 이름이 빈 값이 되고 그 수율(99.75)이 버려졌다. 병합 정보를 보려면 `read_only` 로 열면 안 된다. 기준 줄도 한 줄에 다 있지 않다(조제·충전은 위 줄, 포장은 아래 줄) — 자료 줄 위쪽을 모두 훑어 열마다 하나씩 모으고, 시장별로 갈린 공정은 기준이 하나뿐이면 나눠 쓴다 | `yield_sheet._grid/read_specs` |
| **한 번 읽은 스캔 변경요청서는 다시 읽지 않는다 — 작성 기록에 단계별 시각을 남긴다** (담당자 2026-09-08: "보고서 작성 시간이 기존보다 오래 걸리는 이유가 있는지?") | 12항 스캔 변경요청서 판독은 Claude 를 부르므로 1~2분이 든다. 판독 결과를 제품 폴더의 `PQR 변경요청서 판독.json` 에 남기고, 파일 이름·크기·고친 시각이 그대로면 그 값을 쓴다 (파일이 바뀌면 다시 읽는다). OCR 갈래는 실제로 그 길을 탈 때만 모델을 챙긴다 — 목록을 만들 때 부르면 앞 갈래가 이미 읽었어도 내려받기를 시도한다. 작성 기록의 모든 줄 앞에 `[분:초]` 를 붙여 어느 단계가 느렸는지 기록만 보면 알 수 있게 한다 | `collect._change_by_claude` 되쓰기, `writer.log_` |
| **손글씨 판독은 두 장씩 네 묶음을 동시에** (담당자 2026-09-07: "PQR 하나 만드는 데 너무 오래 걸리네") | 이 PC 의 Claude Code 에 한 묶음씩 차례로 물어 넉 장에 5분이 넘었다. 한 묶음이 실패해도 나머지는 살리고, 다 실패했을 때만 멈춘다. 판독 결과는 판독 파일에 남으므로 **두 번째 작성부터는 다시 읽지 않는다** — 첫 작성 2~3분, 그다음 1분 안팎 | `claude_cli.read_logs` (CHUNK·WORKERS) |
| **읽을 파일이 있는 폴더를 모두 --add-dir 로 연다** (담당자 PC 2026-09-07) | 시험일지가 압축에서 풀려 임시 폴더(%TEMP%\\pqr-engine-…)에 있는데 제품 폴더만 열어 주어, Claude Code 가 "The four PDFs are not readable in this session" 이라며 빈손으로 돌아왔다 | `claude_cli._ask` |
| **글자 없는 스캔 변경요청서는 Claude 로 읽는다 — 공란으로 두지 않는다** (담당자 2026-09-07: "변경요청서 읽으면 돼, PDF 라서 못 읽는 거야?", "못 읽으면 다른 방법을 사용해서라도 읽게 해야지 공란으로 두면 안 돼") | PDF 라서가 아니라 **글자층이 없는 스캔본**이라 글자 판독기로는 한 자도 안 나온다(12.CC-240723-08 은 30쪽 모두 0글자). 빠른 길부터 차례로 시도한다: ① **Claude(API 키)** — 앞 세 쪽을 그림으로 읽는다, ② **이 PC 의 Claude Code**, ③ **이 PC 의 OCR(한글)**. ③ 은 한글 인식 모델(korean_PP-OCRv5_rec_mobile)을 받아 둔 PC 에서만 쓴다 — 프로그램에 딸려 오는 모델은 중국어·영어용이라 '변경관리' 가 'H百尼昆昆用' 처럼 뭉개진다(담당자 2026-09-07: "OCR 로 변환해서 읽으면 안 되는 거야?"). 글자 판독이 터진 경우에도 같은 갈래를 태우고, 모두 실패하면 어느 길에서 왜 못 읽었는지를 문의 목록에 적는다. 판독기가 하나도 없으면 `PQR-Claude설치.bat` 을 알려 준다 | `collect._change_by_claude`, `vision_claude.read_change`, `claude_cli.read_change`, `readers.change_ocr`, `handwriting.korean_engine` |
| **올해 자료가 없어 전년도 결재본에서 옮긴 값은 노랑으로 표시한다** (담당자 2026-09-07: "지금 기록서가 없어 내용이 없거나 이상하면 전년도 PQR 정보로 가져오고 노랑 MARK 해") | 6항 제조단위·포장단위, 8.1.1~8.1.3 의 규격·제조원·평가문서·완료일이 그렇다. 노랑은 **'아직 작년 것'** 이라는 뜻이므로, 올린 자료(공 기록서·공급업체 목록)로 갱신된 칸은 값이 달라져 표시되지 않는다 — 그래서 갱신이 다 끝난 뒤에 견주어 칠한다. 문의 목록에도 몇 칸이 남았는지 적는다 | `recipe_ointment._mark_carried_cells`, 6항 `fill_mfg` |
| **스캔 허가증은 문의 목록에 올리지 않는다** (담당자 2026-09-07: "허가증은 빼자, 의미가 없네") | 3항 허가증은 글자 없는 스캔본으로 오는 일이 잦고, 값은 결재본 것이 맞다. 해마다 같은 글이 문의 목록만 채우므로 작성 기록에만 한 줄 남기고 문의로는 올리지 않는다 | `collect` 3항 |
| **판독 단추는 끝난 뒤에도 '판독됨 ✓ 09-07 20:33' 으로 남는다** (담당자 2026-09-07: "안정성 판독이 완료된 건지 아닌지 모르겠네") | 단추는 눌렀다 돌아오면 다시 '안정성 판독 🔍' 이 되어, 읽은 것인지 아직인지 알 길이 없었다. 제품 폴더의 `13. 안정성시험일지 판독.json` 을 화면 자료에 실어(`build.stability_reading`) 단추 글씨·귀띔에 읽은 시각·Lot 수·애매한 칸 수를 보인다. 판독이 끝나면 화면을 다시 세워(rebuild) 그 자리에서 바뀐다 | `build.stability_reading`, `index.html readButton` |
| **판독이 실패하면 무엇을 보고 왜 지나쳤는지 말하고, 기록을 폴더에 남긴다** (담당자 2026-09-07: "안정성 json 파일이 보이지 않아") | 알림은 6초면 사라져 까닭을 놓친다 — 실패 알림은 1분 남기고, 13 항에서 본 파일을 까닭과 함께 늘어놓는다('13. 안정성 시험 - 개인.mht — PDF 가 아닙니다'). 그와 별개로 제품 폴더에 `PQR 안정성 판독 기록.txt` 를 남긴다. 이름을 'PQR' 로 시작해야 한다 — '13.' 으로 시작하면 그 파일이 13항 자료로 셈해진다 | `stability_read.looked/save_log`, `build.SIDE_FILES` |
| **판독이 끝나면 5초 뒤 저절로 '보고서 작성' 으로 넘어간다** (담당자 2026-09-07: "판독 완료 후에 보고서 작성 버튼을 누르지 않아도 자동으로 넘어갈 수 있나") | 자료 수집 100% 이고 아직 완성본이 없을 때만이다. 알림에 '그만두기' 를 두어 되돌릴 수 있고, 담당자가 먼저 '보고서 작성' 을 누르면 시계는 멈춘다(두 번 돌지 않는다). 그 줄이 화면에 없어도(다른 갈래를 보고 있어도) 작성은 시작한다 | `index.html chainToReport/stopChain` |
| **제품 하나를 끝내는 차례: ① 파일 한번에 올리기 → ② 안정성 판독 🔍 → ③ 보고서 작성 → ④ 문의 목록·노랑 칸 확인** (담당자 2026-09-07: "앞으로 다른 제품 PQR 작성할 때는 파일 한번에 올리기 → 안정성 판독 → 보고서 작성 이렇게 연속으로 하면 되는지?") | ① 은 수집률 100% 가 되어야 ③ 이 열린다 — (v) 0. 당해 년도 공양식과 (u) 16. 전년도 결재본을 빠뜨리기 쉽고, 이력 없는 항은 '이력 없음으로 마감'. ② 는 13 폴더에 손글씨 시험일지가 있을 때만이고, 건너뛰어도 ③ 이 대신 읽는다(그만큼 오래 걸린다). ④ 노랑 칸은 프로그램이 확신하지 못한 값이니 원본과 대조해 고치고, 고친 값은 재작성에도 그대로 쓰인다 | `docs/pqr/index.html` 제품 줄 단추, `collect` 13항 |
| **오늘 준 지시는 대시보드 'PQR 작성 시 참고 사항' 에서 바로 볼 수 있어야 한다** (담당자 2026-09-07: "오늘 수정 요청한 모든 PQR 관련 내용도 PQR 작성 주의사항에 반영해야 돼") | 지시는 **이 대장 한 곳에만** 적고, 화면은 그것을 읽어 장별로 보여 준다(`/api/notes`) — 두 곳에 나눠 적으면 한쪽이 반드시 뒤처진다. 찾기 상자로 '금속성이물'·'13항'·'들여쓰기' 처럼 낱말로 걸러 볼 수 있다 | `server._handle_notes`, `docs/pqr/index.html` renderNotes |
| **13항 손글씨 판독은 '안정성 판독 🔍' 단추로 따로 돌린다 — 누른 뒤 '보고서 작성'** (담당자 2026-09-07: "여기에 13. 안정성시험일지 판독.json 을 만드는 단추를 만들어서 누르고, 완성되면 보고서 작성 단추를 누르게 하면 안 될까?", "'안정성 판독' 단추를 유지하자") | 판독은 길게는 한 시간이라 보고서 만들기와 떼어 둔다. 차례는 API 키 → 이 PC 의 Claude Code 까지만 저 혼자 돌고, **이 PC 판독기(RapidOCR)는 담당자가 한 번 더 고를 때만**(한 장에 3분·20장 71분 실측; 담당자 2026-09-07: "시간이 너무 걸려서 PC 로 안 읽기로 한 거 아냐?") — 고르지 않으면 판독 요청 묶음을 만들고 Cowork·웹 두 길을 알려 준다. 진행 알림은 지금 **무엇이 읽고 있는지**(Claude API·이 PC 의 Claude Code·이 PC 판독기) 그대로 적는다. 만든 판독 파일에 `covers_all: true` 를 적어 '보고서 작성' 이 다시 읽지 않게 하고, 담당자가 고쳐 둔 값은 언제나 먼저다. 진행 줄은 보고서 작성과 같은 자리에 보인다 | `engine/stability_read.make_reading`, `server._handle_read_stability`, `docs/pqr/index.html` readStability |
| **13항 판독은 '그 PC 에서 도는 Claude' 에게 시킬 수 있다 — Cowork·Claude Code 데스크톱** (담당자 2026-09-07: "Cowork 도 내 PC 에 접근 가능하잖아") | 맞다. 담당자 PC 에서 도는 Claude(Cowork·데스크톱·터미널)는 제품 폴더를 바로 읽는다 — 올릴 것 없이 "13 폴더의 안정성 시험일지를 읽어 '13. 안정성시험일지 판독.json' 을 만들어 줘" 한 줄이면 된다. 웹 대화(claude.ai/code 원격 세션)만 PC 를 볼 수 없다. 읽어주세요.txt·문의 목록·진단에 이 두 길을 나눠 적는다 | `handreq.GUIDE`, `collect` 13항 ★, `doctor.report` |
| **판독 요청 묶음은 없으면 그 자리에서 만들어 준다 — 웹 대화 길은 언제나 열려 있다** (담당자 2026-09-07: "웹 대화에 zip 올리기, 이 방식으로도 가능한 거지?") | 이 PC 의 Claude Code 로 읽는 길을 쓰면 묶음이 만들어지지 않는다. 그래도 담당자가 웹 대화 길을 고를 수 있으므로 `/api/reading-request` 가 묶음이 없으면 13 폴더를 훑어 그 자리에서 만든다. 묶음 없이 시험일지 PDF 를 그대로 대화에 올려도 된다 | `server._handle_reading_request`, `handreq.make_request` |
| **13항 판독 요청 묶음은 대시보드에서 바로 내려받는다** (담당자 2026-09-07: "끌어다 놓지 않고 네가 내 PC 에 접근해서 읽으면 안 돼?") | 대화의 Claude 는 회사 밖에서 돌아 담당자 PC 안을 볼 수 없다. 그래서 ★ 문의를 결과 알림 맨 위 빨간 상자로 띄우고, zip 이 있으면 '⬇ 판독 요청 묶음 내려받기' 단추를 붙인다(`/api/reading-request?product=…`). PC 안에서 자동으로 읽히게 하려면 그 PC 에 Claude Code 를 깔거나(`claude -p` 길), `PC 판독 사용.txt` 를 둔다 | `server._handle_reading_request`, `docs/pqr/index.html` star-note |
| **9.1 금속성이물 '합계'·'개개' 칸은 가운데 맞춤** (담당자 2026-09-07: "금속성이물 합계/개개 추가된 글씨는 가운데 맞춤으로 정정", 내수·수출 두 표 모두) | 9.1 은 허용기준 열(끝에서 두 번째)을 왼쪽 맞춤으로 두는데, 그 자리에 든 '합계'·'개개' 는 허용기준이 아니라 결과 칸 이름이라 가운데로 맞춘다 | `layout.align_91_columns` |
| **9.1 결과 칸은 결재본 차림새 그대로** — 평균과 범위는 두 줄('Av. 3.64 g' / '(3.60 ~ 3.67 g)'), 금속성이물은 결과 칸 왼쪽 쪽칸에 '합계'·'개개' 를 적고 값은 평균 하나('0 개'·'0 매') (담당자 2026-09-07: "질량 용량도 왼쪽과 같은 형태로 작성해 줘", "포장에서의 금속성 이물도 왼쪽을 참고해 줘") | 9.1 금속성이물 합계 줄은 성적서의 '합계', '개개' 줄은 '개개' 값을 읽는다(줄에 '합계' 라는 글이 없어도 '개개' 가 아니면 합계 줄). 단위는 합계 '개'·개개 '매' | `recipe_ointment.summarize/fill_91_labelled/fill_92_labelled` |
| **한 열의 소수 자릿수는 가장 자세한 값에 맞춘다** — 5.1·5 를 5.10·5.00 으로 (담당자 2026-09-07: "소숫점 자리를 왼쪽처럼 통일시켜 줘") | 성적서에 적힌 자릿수를 지우지 않는다(예전에는 %g 로 찍어 5.10 이 5.1, 5.00 이 5 로 줄었다). 요약 줄(최댓값·최솟값·평균)과 9.1 결과 글도 같은 자릿수 | `detail92.unify_decimals`, `recipe_ointment._plain` |
| **글로 적는 결과 열은 최댓값·최솟값·평균·Cpk 를 내지 않는다 — 공양식의 사선을 그대로 둔다** (담당자 2026-09-07: "베트남 생균수 시험 최댓·최솟·평균 Cpk 는 구할 필요 없으니 사선 처리해야 돼", "포장 완료 후 확인 시험도 마찬가지") | '10 CFU/g 미만'·'224-228nm 및 292~296nm에서 흡수극대를 나타냄' 처럼 글이 섞인 열에서 숫자만 뽑아 적으면 뜻 없는 값('10'·'296')이 된다. 숫자(범위·'이상/이하' 포함)로만 적힌 열만 요약한다 | `detail92._numbers_only/_stats` |
| **11항 일탈사항은 제목·'* 일탈 내용'·'* 일탈 원인' 머리만 굵게** (담당자 2026-09-07: "일탈 제목, 내용, 원인은 굵은 글씨 처리해 줘") | 그 아래 설명 문장은 보통 글씨. 12항 변경사항(첫 줄만 굵게)과 같은 결 | `docedit.bold_lines`, `recipe_ointment` 11항 |
| **14·15항에 내역이 없으면 연번도 비운다** (담당자 2026-09-07: "불만이 없는데 연번에 1 표시됨") | 한 줄로 줄인 빈 표는 연번 칸의 '1' 까지 지우고 사선 한 줄만 남긴다 | `docedit.single_blank_row` |
| **제조용수(10.4)는 연고 라인이 있는 1층 설비로 적는다** (담당자 2026-09-07: "내가 검토한 결과 연고라인은 1층에 위치해 있어 제조용수 정보는 왼쪽 내용대로 작성하면 돼") | 적격성평가 현황표의 설비명에 층이 적혀 있다('주사용수 제조 시스템 (1.5t - 1층, 냉주사용수)' · '(2t - 2층)'). 다른 층 설비로 적힌 줄은 같은 종류·같은 시스템의 1층 설비로 바꾸고 문의 목록에 남긴다 — 전년도 결재본이 2층 설비로 적혀 있어도 바로잡는다. 층이 안 적힌 설비(공기조화장치 등)는 건드리지 않는다 | `recipe_ointment.use_our_floor` |
| **추가로 수행한 적격성평가가 있으면 그 최신 문서를 적되, 다른 라인만 해당되는 공사는 넣지 않는다** (담당자 2026-09-07) | 마스터파일의 '사유' 칸을 보고 거른다('액제 라인 리모델링에만 적용 (23)' 은 뺀다, '갭 보완 문서 (19)' 는 넣는다). 이 설비의 IQ·OQ 가 다른 라인 문서뿐이면 빈칸으로 두지 않고 넣되 확인을 남긴다 | `masters.applies`, `recipe_ointment` 우리것() |
| **작성본이 프로그램보다 오래되면 '재작성 필요'** (2026-09) | 업데이트 뒤 옛 파일을 열어 보고 "지시가 하나도 반영 안 됐다"(3항·13항 공란, 7항 비고 병합·8.1.2 엔터·4·16항 줄간격 미반영). 작성 시각 < 프로그램 버전이면 단추가 주황 '재작성 필요 ⚠' 가 되고 재작성 창에 경고를 띄운다. 재작성은 언제나 지금 프로그램의 규칙(이 대장 전부)으로 만든다 | `build.final_report_time`, `index.html staleReport` |

---

## 아직 담당자 확인이 필요한 것

- **13.1 시험 기간** — 평가 연도에 끝난 시점을 모두 적는다. 2026년 디겐타에서 OGX901 3M
  (2025.03.25 완료)을 넣을지 담당자에게 물었고 **넣으라**고 했다(2026-09). 한림 결재본에는
  빠져 있었다.
- **12항 '적용 Lot No.'** — 변경요청서에 어느 Lot 부터 적용인지 적혀 있지 않아 '확인 필요' 로 둔다.
- **손글씨 안정성 시험일지** — PC 의 오프라인 판독기(RapidOCR)가 읽고, 읽지 못한 칸만
  노랑·주황으로 남긴다. `ANTHROPIC_API_KEY` 가 있으면 그것이 더 정확히 읽는다. 판독 결과
  (`13. 안정성시험일지 판독.json`)가 제품 폴더에 있으면 그것을 먼저 쓴다.
